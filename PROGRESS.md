# COACHWRIGHT — PROGRESS LEDGER
> # ⚠️ FROZEN ARCHIVE — do not edit, do not append
>
> **This file is history, not state.** It covers sessions S1–S15 and is kept because the reasoning behind
> old decisions is genuinely valuable. Several entries are single 5,000-character paragraphs.
>
> **Do NOT read this file front-to-back.** Grep it when you need the story behind one decision.
>
> **Current state lives here instead:**
> - `strongsuit-v0.1/strongsuit/docs/STATUS.md` — what's true right now (read this first)
> - `strongsuit-v0.1/strongsuit/docs/ROADMAP.md` — what's left
> - `strongsuit-v0.1/strongsuit/docs/DEBT.md` — open debts
> - `strongsuit-v0.1/strongsuit/docs/sessions/` — new session logs go here, one small file each
> - `AGENTS.md` — the protocol every tool follows
>
> This root copy had already **silently diverged** from the app-dir copy by S15 — the two disagreed about
> what had shipped. That is why live docs now have exactly one home. See `AGENTS.md` §2.

Updated: 2026-07-26 · Built by: Claude Fable 5 (sessions 1, 6–8, 10, 13), Antigravity (sessions 2–5), unlogged session (S9 — see note below), Claude Sonnet 5 (S11), S12 (branding/printouts), Claude Opus 5 (S14) · Spec: `STRONGSUIT_MASTER_SPEC.md` (authoritative; lives in `strongsuit-v0.1/strongsuit/`)

> **S14 (2026-07-26): all nine phases are now complete.** Phase 9 (hardening & release) closed out the queue. Two Phase 9 checklist items were deliberately NOT faked and remain open as debt #58 (Lighthouse + cross-browser — this environment has one browser engine and no Lighthouse CLI), and one queue item was declined with reasons as debt #59 (live sync in the static HTML export). Everything else in `HANDOFF_SONNET.md` §4 is done. **The one thing no AI session can do for you is T2: running the packaged Windows installer and the Android build on real hardware.** The Electron app itself has now had its first genuine GUI launch (S14), but `npm run build:electron` → NSIS installer, and the Android/Capacitor build, still need a human.

> **S9 note:** a session ran between S8 and S10 without updating this ledger (violates the Update protocol at the bottom of this file — do not repeat that). S10 reverse-engineered what it built by reading the diff: EULA gate, coach message log, print stylesheet, a full Electron desktop shell, and a first pass at WiFi/cloud sync. All of it is now inventoried below, bug-fixed, and covered by the Update protocol going forward. **If you're an AI picking this up: update this file every session, even a short one — the whole multi-agent relay depends on it.**

> **Product renamed Strongsuit → Coachwright (S8, 2026-07-16)** — "Strongsuit" was trademarked by another (~$10M) company. Brand lives in `src/lib/brand.ts`; user-facing strings pull from it. Data-level ids (IndexedDB name `strongsuit`, backup `app` id) keep the legacy value on purpose so existing data + old backups survive. Old `.strongsuit`/`SSENC1` backups still import. Spec/doc filenames still say STRONGSUIT; the product is Coachwright.

> **Marketing reference doc added (S11, 2026-07-21):** `PRODUCT_OVERVIEW.md` (repo root) is now the single place with full feature lists, pricing, positioning, and ready-to-use copy for BOTH apps (Coachwright + Companion) — written for making posts/store listings/landing copy, not for engineering handoff. It synthesizes `BRANDING_PLAN.md`, the pitch deck (`Coachwright Pitch Deck.dc.html`, found already at repo root — a real, finished-looking investor/buyer brief from an untracked prior session, same pattern as S9), `STRONGSUIT_MASTER_SPEC.md`, and this file. **Flags one open reconciliation, doesn't resolve it silently:** the spec/branding-plan frame the coach license as a $59–99 *test range*, while the pitch deck states a settled $60 — confirm which is actually final before publishing anything with a price on it. Also found two empty, long-abandoned scaffold directories at repo root (`client-pwa/`, `cloud-relay/` — no real source, just stray `package-lock.json`/`node_modules`) from what looks like an even earlier false start, predating `companion-app/`/`sync-server/`; harmless, not touched, worth a cleanup pass sometime (not urgent).

## Phase status
| Phase | Scope | Status | Gate |
|---|---|---|---|
| 0 | Foundation & design system | ✅ **DONE** | Build passes clean; kitchen sink at `#/kitchen-sink` renders all primitives light+dark; zero console output |
| 1 | Data layer & safety | ✅ **DONE** | 7/7 vitest green: AES-GCM round-trip, wrong-passphrase rejection, envelope validation, newer-schema rejection, merge newest-wins, replace-import round trip, e1RM math |
| 2 | Clients | ✅ **DONE** | Roster (search/filter/sort/live), New Client dialog, archive, client workspace shell + Overview tab + injury ribbon DONE. Edit client, Notes tab CRUD, tags editor, typed-name hard delete in Settings, keyboard pass DONE. |
| 3 | Exercise library | ✅ **DONE** | Seed system built + idempotent boot loader; **~200+ of 350 exercises seeded** (`src/db/seed/exercises.ts`). Fuzzy index `src/lib/fuzzy.ts` and dense Library Page implemented with custom exercise creation. |
| 4 | Program Builder | ✅ **DONE** | Builder layout, Undo/Redo history stack, dnd-kit block/exercise reordering, global fuzzy search overlay, spreadsheet-style set entry. |
| 5 | Logging & history | ✅ **DONE** | `SessionLoggerPage.tsx` — full session logger with program-driven day selection, set entry, auto-saved entries, exercise history drawer. `LogsTab.tsx` on client detail. |
| 6 | Analytics | ✅ **DONE** | `OverviewTab.tsx` — weekly frequency chart (pure SVG), session tonnage, e1RM tracking. `MetricsTab.tsx` — bodyweight/measurement charting. Dashboard attention queue live. |
| 7 | Companion + documents | ✅ **DONE** | `companion/template.html` — vanilla JS offline Companion App. `companion/export.ts` — injects brand + payload. `.ssdata` import with ULID-based `mergeUpsert` deduplication. |
| 8 | Operations | ✅ **DONE** | `CheckInsTab.tsx` — manual check-in logging (sleep, mood, energy, adherence, wins/blockers). `BillingTab.tsx` — per-client ledger (payments, session packs, refunds). `CalendarPage.tsx` — appointment scheduling with day-grouped view. `BusinessPage.tsx` — cross-client income stats, monthly SVG bar chart, recent transactions. `ReportsPage.tsx` — cross-client analytics (session/volume stats, PR feed, client roster table). `CommandPalette.tsx` — global Ctrl+K fuzzy search. `OnboardingWizard.tsx` — 4-step first-run wizard. |
| 8.5 | **v1.1 differentiators** (spec §4.16–4.17) | ✅ **DONE** | Film Room (local video analysis: side-by-side/overlay, frame-step, sync lock, line+angle tools). Profit Planner (Expense entity Dexie v3, goal math in `lib/business.ts`, planner UI on `/business`). Progression engine (`lib/progression.ts`: linear/double-progression/RPE policies + policy-free heuristic; "Suggested next" card in ExerciseHistoryDrawer). 24/24 vitest green. |
| 8.6 | **v1.2 intelligence layer** (spec §4.16b, 4.17b, 4.18a, 4.18b) | ✅ **DONE** | Movement tracking AI (MediaPipe pose, fully bundled/offline, lazy chunk): skeleton overlay, joint angles, rep/tempo/depth/symmetry. Nutrition engine + client Nutrition tab (Mifflin-St Jeor→macros, cited rationale per number). Readiness score card on Check-ins. Gym cut per client (percent/flat) through Profit Planner math. Warm-up ramp in history drawer. 38/38 vitest green; all verified live in browser. |
| 8.7 | **v1.3 rename + Film Room dual controls + in-app manual** | ✅ **DONE** | Renamed to **Coachwright** via `src/lib/brand.ts` (data-safe, backward-compatible backups). Film Room: independent per-video transports (Client + Reference each with play/step/scrub), Lock-sync collapses to one master bar, Flip client/ref (horizontal mirror). Full **Guide & tutorial** (11 accordion sections) in Settings. 39/39 vitest green; verified live: rename everywhere + data survived, independent play, sync-lock, tracking still loads offline, zero console errors. |
| 8.8 | **v1.4 (S9, reconstructed)** — EULA, messages, print, Electron, first-pass sync | ✅ **DONE, bug-fixed in S10** | EULA gate (`EulaScreen.tsx`, blocks app until accepted). `CoachMessage` entity + `MessagesTab.tsx` (SMS/email/WhatsApp/in-person log, schema v5). `PrintSessionSheet.tsx` — real printable program sheet (`/print/program/:clientId/:programId`, sibling route outside Shell). **Electron desktop shell** (`electron/main.ts`+`preload.ts`, contextIsolation on, nodeIntegration off, nav-lockdown) — this is the Windows app. First-pass **secure sync**: ECDH+AES-GCM device pairing (from S9's own work, extending S9's crypto) plus a local Electron-hosted WiFi sync server (`WiFiSyncDialog.tsx`, QR-code pairing) and an optional external `sync-server/` (Express+SQLite relay prototype, sibling dir to the app — NOT wired to a hosted service, self-host only). |
| 8.9 | **v1.5 (S10)** — enterprise suite: team, CRM, invoicing, automations, leaderboards | ✅ **DONE** | Reacting to a "build the TrueCoach/Trainerize feature list" ask: built everything that fits the zero-backend doctrine for real, documented (not faked) everything that doesn't. **Team & locations** (`/team`, `Staff`/`Location` entities, commission math). **CRM/Leads** (`/leads`, kanban pipeline, convert-to-client). **Leaderboards & challenges** (`/leaderboard`, opt-in ranking on volume/sessions/bodyweight-loss). **Progress photos** (resized client-side, Metrics tab) + **habit tracking w/ streaks** (Check-ins tab). **Invoicing, coupons, account balances** (Billing tab — numbered invoices, coupon math, "Pay now" bring-your-own-payment-link button). **Automations rule engine** (`Settings → Automations`, configurable local rules; Dashboard attention queue now driven by it instead of a hardcoded 7-day check). **TV Workout mode** (`/tv/:clientId`, full-screen gym-floor display). `docs/SERVER_STRATEGY.md` (honest payments/email/SMS/push/e-commerce strategy — bring-your-own-account patterns, nothing faked) and `docs/ANDROID_STRATEGY.md` (Capacitor chosen + scaffolded; `android/` project genuinely generated but **unbuilt/untested** — no Android SDK in the build environment). 75/75 vitest green. |
| 8.10 | **v1.6 (S11)** — enterprise-suite expansion pt.2, portability, brand identity, bug sweep | ✅ **DONE** | Large multi-part ask, built hardest/most-valuable first: (1) **Feature visibility** — `Trainer.hiddenModules` + `ModuleKey`, a Settings `ModulesCard` checkbox grid, `Shell.tsx`/`CommandPalette.tsx` both filter nav by it (independent coaches can hide Team/CRM/Leaderboards/etc they don't need). (2) **3-tier cloud hosting UI** — `CloudCard.tsx`: fully-local (free) / self-hosted relay (free, coach runs it) / managed-by-us ($15/mo, license-key field) — `Trainer.cloudTier`/`managedLicenseKey`, explicit copy that managed hosting is a separate subscription from the one-time app price. (3) **Robust exercises** — `Exercise.videoLinks[]` (multiple labeled links, legacy `videoUrl` still merges via `lib/videoEmbed.ts`'s `exerciseVideos()`), in-app playback (`VideoViewer.tsx`: YouTube/Vimeo embed, native `<video>` for direct files, explicit Open fallback — never a silent new-tab). (4) **Rest timer** — `RestTimer.tsx`, WebAudio beep countdown, wired into `SessionLoggerPage`. (5) **Biomechanics expansion** — `lib/pose.ts` gained `repConsistency`/`cvScore` (depth+tempo consistency via coefficient of variation) and `barPathPoint`/`barPathDeviation` (wrist-path drift tracking); Film Room got a Bar-path toggle + Movement Analysis consistency stats. (6) **Nutrition expansion** — `lib/nutrition.ts` gained `carbCycle` (training/rest-day macro cycling) and `dietBreakAdvice` (cited recommendation past 8/12 weeks in a cut); both surfaced on `NutritionTab`. (7) **Research-backed metric presets** — `lib/metricPresets.ts` (4 cited testing batteries, goal-filtered), preset chips on `MetricsTab`, `MetricType` expanded (performance/recovery/strength-test). (8) **Client/coach data portability** — `db/portability.ts`: `exportClientPackage`/`importClientPackageText`/`exportStaffClientBundle`, re-keys every row with fresh ULIDs on import so a client's full history can move to a brand-new coach's install (a departing staff member's whole book of clients bundles into one file too). (9) **Competitor CSV roster import** — `lib/csv.ts` (pure RFC4180-ish parser + column-mapping guesser) + `ImportCsvDialog.tsx`: a coach maps any platform's export (TrueCoach, Trainerize, a plain spreadsheet) to Coachwright fields with a live preview — no per-platform schema hard-coded. (10) **Real brand identity** — `public/favicon.svg` was a leftover generic purple-blob placeholder unrelated to the app's actual verde/ember/iron palette; replaced with a real mark (`Logomark.tsx`: three ascending bars + a PR-tag accent dot) used in the sidebar, EULA, onboarding, and a new boot screen. (11) **Boot sequence fix** — `main.tsx` used to fire theme/seed boot logic fire-and-forget while mounting the router immediately, a real race where pages could query IndexedDB before the exercise seed finished; `AppRoot.tsx` now gates the router behind boot completion, `BootScreen.tsx` fills the wait. (12) **Bug sweep** — Film Room's tracker didn't reset its internal (module-level, monotonic) timestamp guard when a clip was replaced mid-tracking-session (fixed); ~10 more files found with non-existent Tailwind classes silently no-op'ing, incl. two (`DashboardPage.tsx`/`ExerciseHistoryDrawer.tsx`) using an `animate-slide-left`/`shadow-sheet` that were never even defined — added the real keyframe/shadow to `tailwind.config.js` rather than deleting the intended slide-in effect (see debt #20, this bug class is recurring, not closed). 37 new tests (`lib/expansion.test.ts` ×16, `db/portability.test.ts` ×9, `lib/csv.test.ts` ×12) → **112/112 vitest green**, `tsc -b` clean. (13) Over-the-server messaging + poll-based reminders + per-coach API keys built into `sync-server/server.ts` and **verified live** (ran the server, curled every endpoint: message directionality, cross-tenant 403s, reminder due/sent transitions, legacy-key back-compat all confirmed correct) — coach-side wired (`features/sync/messageRelay.ts`, `MessagesTab.tsx` Live panel). (14) `docs/SERVER_STRATEGY.md` rewritten (§2.5) to formalize the 3-tier hosting model as doctrine. (15) Full brand-mark variant family + coach-chosen sidebar logo, replacing the earlier single hardcoded mark — see debts #30–32 for two more real Film Room bugs fixed after this (tracker auto-recovery, first-reps-ignored) and the corrected "Full lockup" layout. 113/113 vitest green. |
| 8.11 | **v1.7 (S12)** — branding, printouts, engine UI, responsive polish | ✅ **DONE** | **Brand mark** updated to the C-Collar design in `Logomark.tsx` and `favicon.svg`. **Printable Documents** added via sibling routes (`/print/*`) for Progress Reports, Intake Sheets (PAR-Q+), and Message Digests. **Progression Engine UI** completed: added `ProgressionPolicy` editor in Program Builder and Ghost Suggestions in `SessionLoggerPage`. **Coaching Message Log** completed: "From your coach" injected into the HTML Companion app payload. **Responsive UI pass** completed: collapsible sidebar added to `Shell.tsx`, responsive stacking added to `ProgramBuilder.tsx` and `FilmRoomPage.tsx`. |
| 8.12 | **v1.8 (S11, continued)** — real dual-clip tracking + the Companion client app (new product line) | 🟡 **IN PROGRESS, honestly scoped** | Film Room can now track BOTH clips at once (see debt #47) with a rep-by-rep + joint-angle comparison breakdown — closes the "isn't tracking them side by side" gap for real. **New:** `docs/CLIENT_APP_STRATEGY.md` — architecture + pricing doctrine for a second, client-facing app (Companion), local-first by default, syncing over the COACH's relay when paired (never the client's own money), plus an optional client-only "Personal Cloud" tier ($3.99/mo·$29/yr) for a client who wants cross-device sync of their own data without a coach. New sibling project `companion-app/` scaffolded for real (build-order steps 1–2): PWA shell, bottom-tab nav, onboarding, standalone workout logging, body-metric tracking, Personal Cloud pricing card. **Step 3 (coach-pairing sync) is now REAL end-to-end across all three coach hosting tiers** — see debt #52 and #53: genuine ECDH pairing with a spoken safety-number confirmation; real `/sync/push` + `/messages/push`+`/pull` for self-hosted/managed coaches; a file-based `.cwsync` export for fully-local coaches that drops straight into the coach app's *existing* "Local Import" button; and a single coach-side `remapClientId()` fix (`syncApi.ts`) that makes both transports file synced data under the right `Client` correctly — one merge function, one payload shape, three transports. `docs/CLIENT_APP_STRATEGY.md` §7 documents the unified design. **S13 closed the loop the rest of the way:** coach→client program/message delivery is now real over ALL transports (relay pull, WiFi/LAN direct-to-desktop, imported file — one merge function, `applyCoachPacket`), Companion gained a Program viewer + a dedicated Coach messaging tab, messages ride sync packets both directions (messaging now works even for a fully-local coach), and the S9 LAN sync loop's silently-broken IPC response path was fixed. See the S13 session-log entry + debts #54–55. **Still open** (build-order step 5): porting Film Room self-review into the client app (T13); log-against-assigned-program prefill. |
| 9 | Hardening & release | ✅ **DONE (S14)** — one item deferred, honestly | **First-boot hang fixed for real** (debts #6/#54a — `singleFlight` + insert-race tolerance in `trainerRepo.getOrCreate`/`seedExercisesIfEmpty`, plus a boot **error screen with Retry** so no failure can ever hang on a frozen progress bar again; 11 new tests). **Per-route error boundaries** (`RouteError.tsx` as every route's `errorElement`) — a crashing page now renders inside the Shell with nav intact instead of whiting out the app. **Code splitting**: main chunk **820KB → 476KB** (150KB gz), under the 500KB warning for the first time; every route except Today/Clients is its own lazy chunk, and the ~280-row exercise seed is only fetched on the one boot that needs it. **PWA**: hand-rolled `manifest.webmanifest` + `sw.js` + maskable icons (vite-plugin-pwa still blocked by the Vite 8 peer cap, debt #49) — **verified genuinely offline by killing the server and reloading**. **Reduced-motion pass** — the pre-existing rule was incomplete (infinite animations kept looping at ~0ms = a busy blur; delays survived so `both`-filled boot text sat invisible); now caps iterations, zeroes delays, keeps a slowed spinner, and skips the decorative boot reveal entirely. `HOW-TO-OWN-IT.md` written. Debt #19 (committed relay `.db`) and #27 (orphan sprite) cleared. **Deferred:** Lighthouse + cross-browser runs (needs browsers this sandbox doesn't have — see debt #58). |

## Feature checklist
- [x] 4.1 Design system (Phase 0)
- [x] 4.2 Data layer & safety (Phase 1)
- [x] 4.3 Client management (Phase 2)
- [x] 4.4 Exercise library (Phase 3)
- [x] 4.5 Program builder (Phase 4)
- [x] 4.6 Session logging & history (Phase 5)
- [x] 4.7 Companion app & documents (Phase 7)
- [x] 4.8 Dashboard & attention queue
- [x] 4.9 Check-ins & intake (`CheckInsTab.tsx`)
- [x] 4.10 Analytics (`OverviewTab.tsx`, `MetricsTab.tsx`)
- [x] 4.11 Calendar (`CalendarPage.tsx`)
- [x] 4.12 Business ledger (`BusinessPage.tsx`, `BillingTab.tsx`)
- [x] 4.13 Cross-client reports (`ReportsPage.tsx`)
- [x] 4.14 Data portability (backup/restore + Companion export/import)
- [x] 4.15 Command palette (`CommandPalette.tsx`)
- [x] 5. First-run experience (`OnboardingWizard.tsx`)
- [x] 4.16 Film Room (`filmroom/FilmRoomPage.tsx`, route `/film-room`)
- [x] 4.17 Profit Planner (`lib/business.ts`, `BusinessPage.tsx`, `expenses` table)
- [x] 4.14 Progression engine core (`lib/progression.ts` + drawer surface) — policy editor in builder still pending
- [x] 4.16b Movement tracking AI (`lib/pose.ts`, `filmroom/tracker.ts`, bundled MediaPipe in `public/mediapipe/`)
- [x] 4.17b Gym cut per client (`Client.gymCut`, BillingTab editor, planner math)
- [x] 4.18a Nutrition engine (`lib/nutrition.ts`, `clients/NutritionTab.tsx`)
- [x] 4.18b Readiness score (`lib/readiness.ts`, CheckInsTab card)
- [x] Rename to Coachwright (`lib/brand.ts` single source; data-safe)
- [x] Film Room dual per-video controls + flip (`FilmRoomPage.tsx`: `useClip`, `TransportBar`)
- [x] In-app Guide & tutorial (`features/settings/Guide.tsx`)
- [x] 4.8 Printable/branded documents (`PrintSessionSheet.tsx`, S9) — program sheet only; progress-report PDF and blank intake sheet still open
- [x] 4.19 Coaching message log (`CoachMessage`, `MessagesTab.tsx`, S9)
- [ ] 4.18 Nutrition full food-log module (v2 candidate, not built — targets engine (4.18a) is done)
- [x] PWA manifest + service worker (Phase 9, S14) — hand-rolled; verified offline with the server stopped
- [x] EULA gate (`EulaScreen.tsx`, S9)
- [x] Electron desktop shell = the Windows app (`electron/`, S9) — packaged installer not yet produced, see debt #18
- [x] Secure sync: local WiFi (Electron-hosted) + device pairing (S9/S10) — see debt #16 for the still-open half
- [x] Team & locations, CRM/leads, leaderboards/challenges, progress photos, habit tracking, invoicing/coupons/balances, automations engine, TV Workout mode (all S10 — see phase 8.9 row)
- [x] `docs/SERVER_STRATEGY.md`, `docs/ANDROID_STRATEGY.md` (S10)
- [ ] Android: scaffolded (`android/` via Capacitor) but **never built or run** — no SDK in any build environment so far; needs a human with Android Studio (see debt #17)
- [x] Feature-visibility toggles (`Trainer.hiddenModules`, `ModulesCard`, S11)
- [x] 3-tier cloud hosting settings UI (`CloudCard.tsx`, S11) — doc rewrite to match still pending, see debt #22
- [x] Multi-link in-app exercise video (`lib/videoEmbed.ts`, `VideoViewer.tsx`, S11)
- [x] Rest timer (`RestTimer.tsx`, S11)
- [x] Biomechanics: rep consistency (CV) + bar-path drift (`lib/pose.ts`, S11)
- [x] Nutrition: carb cycling + diet-break advice (`lib/nutrition.ts`, S11)
- [x] Research-backed metric presets (`lib/metricPresets.ts`, S11)
- [x] Client/coach data portability (`db/portability.ts`, S11)
- [x] Competitor CSV roster import (`lib/csv.ts`, `ImportCsvDialog.tsx`, S11)
- [x] Real logomark + boot screen (`app/brand/Logomark.tsx`, `AppRoot.tsx`, `BootScreen.tsx`, S11) — replaces the leftover placeholder favicon
- [x] Full brand-mark variant family + coach-chosen sidebar logo (`BRAND_MARK_VARIANTS`, `BrandMark`, Settings `BrandMarkCard`, S11) — matches the reference brand-guide sheet exactly, dark-mode-safe
- [x] Electron splash screen actually wired up + rebranded (`electron/splash.html`/`main.ts`, S11) — was dead code before, never displayed
- [x] Program Builder critical bug fix: empty-day "Add Exercise" silently failed for every user (`DayCanvas.tsx`, S11 — see debt #28)
- [x] Film Room reliability + occlusion hardening + sync-drift correction + bar-path throttling (S11 — debts #33–37)
- [x] Film Room track-either-clip selector, "no person detected" hint, timestamped notes + natural-language client export (S11 — debts #38–40)
- [x] Real month-grid Calendar view (`MonthGrid`, Month/List toggle, `CalendarPage.tsx`, S11 — debt #42) — was agenda-list-only before
- [x] Over-the-server messaging + poll-based reminders on `sync-server/` + per-coach API keys (S11) — server verified live via curl; coach-side client wired (`messageRelay.ts`, `MessagesTab.tsx`). Companion (client) side still open, see debt #23
- [x] `docs/SERVER_STRATEGY.md` 3-tier-hosting rewrite to match `CloudCard.tsx` (S11)
- [x] Full responsive/dynamic-sizing pass (S14) — the surfaces T10 flagged as genuinely unverified (Film Room's dual-video stage, Business/Billing, Settings' brand-mark grid, dense client tables) all checked at 375px **and** 768px: no horizontal overflow anywhere, dual video stacks vertically, no clipped or sub-28px controls
- [x] Electron native-menu + window-state persistence polish (S14, T11) — File/Edit/Go/View/Window/Help menu driving the router over IPC, window geometry persisted + restored with an off-screen-monitor guard; **verified in the first real Electron GUI launch this project has ever had**
- [x] Route error boundaries + code splitting + reduced-motion (Phase 9, S14)
- [x] Film Room per-rep table + PNG snapshot export (S14, T6b) — CommandPalette registration (T6d) was already done in an untracked pass
- [x] Coach-side reminder scheduling UI (S14, closes debt #56) — schedule/cancel/upcoming, E2EE-verified against a live relay
- [x] Companion: Film Room self-review (S14, T13) — free, on-device, no coach required

## What works right now (verified)
- `npm run build` → clean production build (main chunk ~820KB/~247KB gz + a lazy 135KB MediaPipe chunk), fonts self-hosted, zero runtime network calls in the core app
- `npx vitest run` → 146/146 passing
- Full app shell: left rail grouped into Core + Studio sections w/ verde spine, backup-health indicator (live, turns ember at 7d)
- Dashboard: onboarding checklist (auto-checks), needs-attention queue now driven by the **Automations engine** (configurable rules + 2 always-on defaults)
- Clients: add → appears live → open workspace → archive; injury ribbon renders; mailto:/sms: quick-contact links when email/phone present
- Client detail: Overview, Coaching, Program, Logs, Check-ins, Metrics, Nutrition, Notes, Messages, Billing tabs all functional
- Settings: brand kit, Automations rule editor, in-app Guide, backup export (plain + encrypted) and restore (merge/replace) fully functional
- Exercise seed loads on first boot (200+ movements with real cues)
- Program Builder: full CRUD, undo/redo, drag-and-drop, set editing
- Session Logger: program-driven, auto-save, exercise history drawer w/ "Suggested next" + warm-up ramp
- Companion App: export → offline HTML → .ssdata import with merge
- Film Room: side-by-side/overlay, independent dual transports, sync-lock, flip, on-device pose tracking (skeleton/reps/tempo/depth/symmetry)
- Calendar: recurring appointments (weekly/biweekly/monthly, multi-weekday, count/until), reschedule-one-occurrence, cancel-one-occurrence
- Business: Profit Planner (goal math + gym-cut), cross-client income aggregation with monthly chart
- Team & locations: staff roster, commission math live against the Ledger
- Leads: CRM pipeline (new→contacted→trial→won/lost), convert-to-client
- Leaderboards: opt-in cross-client ranking (volume/sessions/bodyweight-loss) + time-boxed challenges
- Billing: invoicing (line items, coupons, "Pay now" bring-your-own-link), account balance owed
- Reports: cross-client stats, PR feed, client roster overview
- Command Palette: Ctrl+K global fuzzy search
- Onboarding: 4-step wizard with demo data seeding; EULA gate after onboarding, before first use
- Sync: WiFi/LAN pairing + packet exchange (Electron desktop only for the live server; file export/import works everywhere)
- TV Workout mode: full-screen gym-floor display, keyboard day navigation
- Windows: Electron shell compiles clean, asset paths verified `file://`-safe; full GUI launch + installer build **not** executed in this environment (see debt #18)
- Android: Capacitor scaffold generated and installed; **not built or run** anywhere yet (see debt #17)
- Settings: `ModulesCard` (hide unused nav modules) + `CloudCard` (local/self-hosted/managed-$15permo) live, verified in browser
- Exercise Library: multiple labeled video links per exercise, in-app playback dialog (YouTube/Vimeo embed or native `<video>`)
- Session Logger: rest timer (WebAudio countdown beep) fires on set completion
- Film Room: bar-path drift tracking + depth/tempo consistency stats (coefficient-of-variation based)
- Nutrition tab: training/rest-day carb cycling, diet-break advice (cited, cut goal only)
- Metrics tab: research-backed testing-battery presets, goal-filtered
- Clients: export any client's full history as a portable file (`Export data` on Client Detail); import a single client package, a bundle, or a competitor CSV roster (`Import clients` on the Clients page) — verified via `npx vitest run` (21 tests) since no real TrueCoach export sample was available to test against live
- Boot: `AppRoot.tsx` gates the router behind theme+seed completion, `BootScreen.tsx` shows an animated logomark meanwhile — closes a real race where a page could query IndexedDB before the exercise seed finished
- Brand: real logomark (`Logomark.tsx`) replaces the leftover generic placeholder favicon everywhere — sidebar, EULA, onboarding, boot screen, `public/favicon.svg`
- Cloud relay (`sync-server/server.ts`): messaging (`/messages/push`, `/messages/pull`) + poll-based reminders (`/reminders/schedule`, `/reminders/due`, `/reminders/upcoming`) + per-coach API keys (`/keys/register`, `/keys/revoke`, admin-key gated) — **verified live** by running the server (`npx tsx server.ts`) and curling every endpoint end-to-end (message directionality, cross-tenant 403s, reminder due/not-due/already-sent transitions, legacy shared-key backward compat), not just reading the code. Coach-side client wiring (`features/sync/messageRelay.ts`, `MessagesTab.tsx`'s Live panel) done and typechecked. **Companion (client-facing) side not wired** — see debt #23.
- `docs/SERVER_STRATEGY.md` rewritten (§2.5) to formalize the 3-tier hosting model as doctrine, matching `CloudCard.tsx` — debt #22 closed.
- Film Room: raw landmark positions (not just computed joint angles) now run through a One-Euro-based `LandmarkSmoother` with visibility-weighted trust blending (`lib/pose.ts`) — the on-screen skeleton and bar-path tracking are visibly steadier through brief occlusion, verified via 6 new unit tests plus live DOM verification.
- Boot: two-phase sequence — a genuine determinate progress bar (tied to real boot steps: trainer load → theme → storage.persist → exercise seed) plays first, then the wipe-build mark/wordmark/tagline reveal plays second (`AppRoot.tsx`/`BootScreen.tsx`) — previously a single-phase screen. Verified live (DOM-level, catching both stages via reload timing) since the ~2s total duration is too fast to reliably screenshot.
- Cloud capability gating centralized: `lib/cloudCapability.ts`'s `cloudCapabilities()` is now the single source of truth for what a coach's hosting tier unlocks (sync/messaging/reminders + a plain-language reason when hidden) — `MessagesTab.tsx`'s Live panel and `SyncCenterPage.tsx`'s Cloud Sync button/config card both consume it instead of ad-hoc `!!trainer.syncServerUrl` checks, closing a real inconsistency where `SyncCenterPage` would let a coach "Cloud Sync" even after switching the tier selector back to fully local as long as a URL was still saved.
- In-app Guide (`Guide.tsx`) updated to cover everything shipped this session: CSV import mention, a new Calendar section, Film Room's notes/export/reliability improvements, a new "Cloud sync — optional, in three flavors" section, and a corrected Privacy section (no longer claims "no server, ever" unconditionally now that opt-in relay tiers exist).
- Film Room: `trackTarget === 'both'` runs genuine simultaneous dual-clip tracking (two independent MediaPipe instances, verified via network requests) with a rep-by-rep + joint-angle-deviation comparison card (`lib/filmCompare.ts`) — the "compare client vs. reference" gap (old debt #12/#38) is now closed for real, not just "track one or the other."
- **New product line:** `companion-app/` (`strongsuit-v0.1/companion-app/`) — a separate client-facing PWA. Onboarding, standalone workout/metric logging, and Settings (incl. the Personal Cloud pricing card) all verified live with zero console errors; `npm run dev` at port 5175 (`.claude/launch.json`'s `companion-app-dev` config). **Coach pairing + sync is now real** (debt #52 — superseded the earlier stub, debt #50): genuine ECDH handshake with a spoken safety-number check, real `/sync/push` of logged workouts (coach-app-compatible `SessionLog`/`Metric` shape), and real `/messages/push`+`/pull` fully interoperable with the coach app's existing `messageRelay.ts`. **Verified against a live `sync-server` instance**, not just unit-reasoned: paired two synthetic identities, pushed a workout, decrypted it back on the "coach" side and got the exact data, sent a message, decrypted that too — all real network calls, zero console errors. Read `docs/CLIENT_APP_STRATEGY.md` before continuing this line, especially §3.5.

### Added S14 (all verified live, not just typechecked)
- **First boot no longer hangs.** A brand-new profile boots cleanly to the onboarding wizard, three wipe-and-reload cycles in a row, one trainer row + 277 exercises each time, zero console errors. The old failure mode was proven, not guessed: running the previous check-then-insert pattern concurrently against this browser's real IndexedDB reproduces `ConstraintError: Key already exists` on demand. **The identical bug existed in `companion-app` and was found the same way** — its very first launch rendered a permanently blank page (profile row created, but the losing StrictMode effect's rejection meant `setProfile` never ran); fixed with the same `singleFlight` + adopt-the-winner pattern.
- **A boot failure now shows an error screen with a working Retry**, naming the step that failed ("Failed while seeding the exercise library") — verified by forcing a throw, confirming the screen, clicking Retry and seeing boot genuinely re-run. Same treatment in Companion.
- **Route error boundaries.** Forcing a render crash in `ReportsPage` renders the error card *inside the Shell* with the whole sidebar still clickable; an unknown hash route renders "Page not found" instead of a blank screen.
- **Offline PWA.** Loaded the production build, browsed 8 routes, then **stopped the preview server entirely** (`curl` refused) and reloaded — the app came back fully, served by the service worker, and cached lazy routes still navigated. 34 assets cached.
- **Electron desktop app launched for the first time.** Menu built (`File/Edit/Go/View/Window/Help`); **Go → Clients** moved the renderer's router to `#/clients` through the real IPC path; window resized to 1024×720 at (120,90), quit, relaunched and restored to exactly those bounds. Also caught and fixed a **broken splash screen** — `splash.html` was loaded from `dist-electron/`, where `tsc` never copies it, so every launch logged `ERR_FILE_NOT_FOUND` behind a blank splash window.
- **Scheduled reminders, coach side (debt #56 closed).** Scheduled one through the real UI → confirmed the sealed row in the relay's own SQLite → **independently decrypted it with the synthetic client's private key** to get the exact text back in the `{content}` shape Companion expects → confirmed the client's own `/reminders/due` poll returns it once and only once → cancelled it and confirmed the row was deleted.
- **Companion Film Room (T13).** Loads a clip, initialises the tracker, and runs the detection loop — the wasm runtime and the 5.7MB pose model are fetched from **Companion's own `public/mediapipe/`** (confirmed in network requests), not a CDN and not the coach app. ~360 frames processed with zero exceptions and the "no person detected" hint firing correctly on synthetic footage. MediaPipe stays in a lazy chunk, so the app bundle is unaffected.
- **Film Room PNG snapshot.** Produces a real 320×480 PNG at the video's native resolution with the frame, skeleton and caption composited — verified by decoding the blob back and sampling pixels, not by trusting the download.
- **The HTML Companion export makes zero external requests.** It previously pulled `html5-qrcode` from unpkg — in a file whose entire promise is working offline, and which let a CDN see every time a client opened their own workout. Full loop re-verified: coach messages render, log a set → finish → a valid `.ssdata` file with the right shape.

## Known debts (intentional, do not "fix" silently)
1. Dashboard attention-queue scans all logs in memory — fine now, index-optimize if scale demands.
2. Two `as any` casts at Dexie generic boundaries (`repo/base.ts` update, `backup.ts` table iteration) — documented, contained, acceptable.
3. No ESLint flat-config customization yet — default Vite config only.
4. ~~PWA plugin not yet configured (Phase 9 by design).~~ **CLOSED (S14)** — hand-rolled manifest + service worker in both apps; `vite-plugin-pwa` itself is still blocked by its Vite ^6 peer cap (debt #49).
5. CalendarPage placeholder export still exists in `placeholders.tsx` (dead code, not imported).
6. ~~**First-boot console errors (pre-existing, observed S6)**~~ **CLOSED (S14) — root-caused and fixed, see the S14 session-log entry.** Original text: on a brand-new profile the very first load logs Dexie errors caught by the router error boundary and briefly double-renders the wizard; subsequent loads are clean. Hypothesis: two concurrent `trainerRepo.getOrCreate()` calls both `add()` the singleton row → ConstraintError. Fix candidate: use `put()` or catch ConstraintError in `getOrCreate`. Violates the §6 "zero console errors" gate — fix in Phase 9.
7. Film Room needs a manual QA pass with real phone footage (verified rendering + controls in browser; frame-step/sync/angle math not yet exercised against real video). Not registered in CommandPalette yet.
8. Bundle 657KB (>500KB chunk warning) — code-split Film Room + builder routes in Phase 9.
9. Doc copies exist at repo root AND in `strongsuit-v0.1/strongsuit/` — root copies are authoritative for PROGRESS/HANDOFF/BRANDING; spec is authoritative in the project dir. S6 synced them; keep them synced.
10. Rep counter thresholds (35%/12% of observed ROM, 10-sample warm-up, 25° min range) are unit-tested against synthetic series but **not yet tuned on real phone footage** — expect a tuning pass (and possibly One-Euro landmark smoothing) after manual QA. Depth% uses observed max angle as the "standing" reference, which underreports if the clip never shows full standing.
11. Distribution size grew ~22MB (wasm runtimes ×3 + pose model in `public/mediapipe/`). Phase 9 may prune to SIMD+nosimd only, or lazy-fetch nothing — model must stay bundled (offline doctrine).
12. Tracking analyzes clip A (client) only; reference-clip tracking + angle deltas are a specced extension (§4.16b).
13. Keyboard transport (Space/←/→) drives the master/client video only; the independent Reference bar is mouse-only. Fine for now; note it in any keyboard-QA pass.
14. Spec/doc **filenames** still say STRONGSUIT (STRONGSUIT_MASTER_SPEC.md); content refers to the product as "Strongsuit" in older sections. Not renamed to avoid churn — treat as Coachwright. Consider a one-time rename pass in Phase 9.
15. `favicon.svg` referenced by index.html is the old/default; design a Coachwright "C-collar" favicon in the branding pass.
16. **Sync is only half-connected end to end.** `lib/sync.ts`'s E2EE crypto is solid and tested; `WiFiSyncDialog`'s Electron-hosted local server can receive `/sync/push`, but nothing on the *Companion* (client-facing) side actually calls it yet — the Companion HTML template has no "sync over WiFi" button POSTing to the discovered coach IP. Today the real, fully-working path is manual `.cwsync` file export/import (Studio Link page) or the Companion's existing `.ssdata` file-based return path — both zero-server and complete. Closing the WiFi loop needs: (a) a small vanilla-JS fetch call added to `companion/template.html`, (b) a "which client is this" pairing step. The standalone `sync-server/` (Express+SQLite) prototype is a separate, unwired, self-host-only cloud relay option — not connected to anything by default, and its API key defaults to a hardcoded string (`'default-coachwright-key'`) that MUST be overridden if anyone actually deploys it; document that loudly before ever recommending it to a buyer.
17. **Android is scaffolded, not verified.** `android/` is a real Capacitor-generated project (not a stub), but no Android SDK/Gradle/emulator exists in any environment this has been built in — it has never been compiled, launched, or tested on a device. Treat as "next step ready," not "done." See `docs/ANDROID_STRATEGY.md`.
18. **Windows/Electron is verified statically, not by a live GUI launch.** `electron/main.ts`+`preload.ts` compile clean; `dist/index.html` uses `file://`-safe relative asset paths; `contextIsolation`/`nodeIntegration` are set securely; the Electron binary itself runs (`--version` succeeds). A live window launch (`npm run dev:electron`) and a packaged installer (`npm run build:electron`, NSIS) have **not** been executed in this sandboxed environment — do that once, interactively, before calling Phase 9 done.
19. **CLOSED (S14): `coachwright.db` is now untracked (`git rm --cached`, file left on disk) and `*.db` / `*.db-shm` / `*.db-wal` are gitignored, so it cannot be committed or packaged again.** Original text: `sync-server/` (the standalone cloud relay) has its own `node_modules` and a `coachwright.db` SQLite file already committed to disk from earlier testing — harmless, but don't accidentally ship that populated `.db` file if this ever gets packaged for distribution. **(S11)** Deleted it from the working tree while live-testing the new endpoints (a fresh one regenerates automatically on next `npm start`) — this shows as a pending `git rm` in status; left unstaged since committing wasn't asked for, but it's a reasonable one to actually commit given this exact debt.
20. **Recurring, not closed:** components keep shipping with undefined Tailwind classes (`bg-brand-*`, `text-brand-*`, `bg-iron-50/100/800/900`, `bg-surface-elevated`, `bg-bg`, `verde-500/800/900`) that silently no-op instead of erroring. S10 found+fixed 5 files; **S11 found 10 more** (`Shell.tsx`, `MetricsTab.tsx`, `CheckInsTab.tsx`, `ExerciseHistoryDrawer.tsx`, `BusinessPage.tsx`, `LogsTab.tsx`, `OverviewTab.tsx`, `ExerciseSearch.tsx`, `ExerciseRow.tsx`, `BuilderOutline.tsx`) plus two components (`DashboardPage.tsx`, `ExerciseHistoryDrawer.tsx`) referencing an `animate-slide-left`/`shadow-sheet` that were never defined anywhere (added real ones to `tailwind.config.js` rather than deleting the intended drawer-slide effect). **This is clearly systemic, not one-off** — every AI session that writes Tailwind classes from memory reintroduces a few. Consider `eslint-plugin-tailwindcss` (or similar) in Phase 9 to catch this at lint time instead of by grep-hunting each session; until then, grep `tailwind.config.js`/`src/index.css` before trusting any color/shadow/animation class you didn't just look up.
21. `sessionsRemaining` (used by the `package-low-sessions` automation rule) is an **estimate** — `purchased session-credit total − all-time logged sessions` — not a true decrementing pack ledger (no such first-class concept exists yet). Fine for a nudge; don't present it to a buyer as exact.
22. ~~`docs/SERVER_STRATEGY.md` still frames "managed hosting" as speculative~~ **CLOSED (S11, same session)** — rewritten with a new §2.5 formalizing local/self-hosted/managed as official doctrine.
23. **(S11)** `sync-server/server.ts` now has messaging + poll-based reminders + per-coach API keys (verified live via curl — see "what works" above), and the coach-side client (`messageRelay.ts`, `MessagesTab.tsx`) is wired. **Still open:** the Companion (client-facing) `companion/template.html` — vanilla JS, no framework — has no code yet to push a reply or poll `/reminders/due`; a client can currently only RECEIVE via the coach's own app checking on their behalf, not send back or see reminders on their own device. Also open: `/keys/register` provisioning is a manual operator step (no Stripe webhook wired) — fine at low volume, worth automating later. See HANDOFF T8 (re-scoped to just these two items).
24. **(S11, revised same session)** ~~No responsive audit has been done~~ — partially untrue: `Shell.tsx` already has a working mobile drawer/hamburger header (built before this pass, cause unclear), and live mobile-viewport (375px) testing this session confirmed Dashboard, Clients, Programs list, and the full Program Builder (outline → day canvas → exercise row → set editor) all render and function correctly. **Still genuinely unverified at mobile width:** Film Room's dual-video side-by-side stage (very likely needs a stacked/tabbed layout below some breakpoint — two videos side by side will not fit 375px), Calendar, Business/Billing tabs, the Settings page itself, and dense data tables beyond the Clients roster. Narrow-window Electron (not just phone-width web) is still unverified either way.
25. **(S11)** Electron shell (`electron/main.ts`) has no native menu bar and doesn't persist window size/position between launches — cosmetic, not a correctness bug, but was explicitly asked for and not done.
26. **(S11)** `db/portability.ts`'s client-package export intentionally excludes `invoices`/`expenses`/`challenges` (business-level or multi-client entities) — a ported client's payment ledger and challenge history do not travel with them. Documented scope choice, not an oversight; revisit if a buyer asks for full financial history portability.
27. **CLOSED (S14): deleted.** Original text: `public/icons.svg` is an orphaned social-icon sprite sheet (Bluesky/Discord/GitHub/X/etc, one path using a stray purple `#aa3bff`) left over from the original project scaffold — confirmed unreferenced anywhere in `src/`. Harmless dead weight, not wired to anything; safe to delete whenever someone's doing a cleanup pass, just wasn't in scope for the branding pass that found it.
28. **(S11) FIXED, but watch for the same pattern elsewhere:** `DayCanvas.tsx`'s empty-day "Add Exercise" button called a state-updater (`addBlock()`/`updateDay()`) then, in the same synchronous handler, read the *pre-update* `day` prop to decide `targetBlockId` — silently broke the most common first action in the Program Builder (see session log for the full story). Only this one instance was found and fixed; a broader project-wide search for "call a prop-updater, then read the same prop synchronously afterward expecting it to reflect the update" was NOT exhaustively done beyond this file. If something silently "does nothing" on click elsewhere, check for this pattern before assuming it's something else.
29. **(S11)** MediaPipe's GPU delegate now falls back to CPU on failure (`tracker.ts`), but this was never tested on hardware that actually lacks WebGL2 — only verified that the GPU path works (this sandbox has it) and that the fallback *code path* is structurally sound. Worth a real test on a machine/VM without hardware acceleration before calling it fully verified.
30. **(S11) FIXED — "tracking sometimes breaks."** Two compounding root causes in `tracker.ts`/`FilmRoomPage.tsx`: (a) `detectFrame` swallowed every `detectForVideo` exception silently (returned "no landmarks," indistinguishable from an ordinary clean miss) — if the landmarker ever wedged (GPU context loss, a WASM fault), tracking would just quietly stop producing a skeleton forever, with zero indication anything was wrong; (b) turning tracking off never disposed the cached `landmarker` singleton, so turning it back on (the obvious "try again" move) handed back that same wedged instance instead of a fresh one — the natural recovery action didn't actually recover anything. Fixed: `PoseFrame` now carries an `error` flag distinct from a clean miss; `FilmRoomPage` counts consecutive errors and auto-disposes+reinitializes the tracker after 12 in a row (toasts "Movement tracking hiccuped and restarted itself"); `toggleTracking`'s off-branch now calls `disposeTracker()` so the next start is always genuinely fresh. Not reproduced live (the failure mode needs an actual GPU-context-loss event, not something a sandbox can trigger on demand) — this is a structural fix for a plausible, reported-as-intermittent failure, not a verified-fixed one. If it keeps happening, the next diagnostic step is capturing what `detectForVideo` actually throws.
31. **(S11) FIXED — "the initial reps are always ignored."** Real bug, confirmed with a unit test (`replayHistory rescues a rep that completed before the focus joint was known`, `lib/engines.test.ts`): both `RepCounter` and `FocusJointPicker` need a warm-up window before they'll trust anything (a real observed range for the counter, `>24°` for the joint picker), and angle samples were only ever pushed to `repCounter` *after* a focus joint existed — so any rep that finished during that calibration window (very often the first rep of the set, since that's exactly when the system is still figuring out which joint to track) was fed to nothing and lost forever, on top of `repCounter` then needing its *own* fresh 10-sample warm-up starting from zero. Fixed: `FilmRoomPage` now buffers every frame's full angle set (`angleBuffer`, capped at 600 like the existing bar-path buffer) from the moment tracking starts; the instant a focus joint is first identified, the whole buffer replays through `repCounter` in one shot (`lib/pose.ts`'s new `replayHistory`), retroactively scoring anything that already happened, before live frames resume pushing into the same counter normally.
32. **(S11) FIXED — the "Full lockup" brand-mark variant didn't match the reference sheet.** Built it as a vertically-stacked, centered arrangement (icon on top, wordmark+tagline centered below) — the reference sheet's actual "Primary Lockup" is a horizontal row (icon left, big left-aligned wordmark+tagline to its right, vertically centered on each other, badge:gap:wordmark:tagline ≈ 132:40:59:13px). Rebuilt `Lockup` in `Logomark.tsx` to match those ratios exactly (gap≈0.3×, wordmark≈0.45×, tagline≈0.1× the mark size) — verified visually in both the Settings picker preview and the live sidebar. **Corrected again same session**: the tagline still didn't fit one line at the sidebar's small scale — shrunk its font/tracking further, tightened the icon-to-text and wordmark-to-tagline gaps, reduced the sidebar's `markSize` 40→34.
33. **(S11) FIXED — Film Room reliability.** Two compounding root causes behind "tracking sometimes breaks": `detectFrame` couldn't distinguish a thrown exception from an ordinary clean miss, and turning tracking off never disposed the cached tracker, so "turn it off and on again" didn't actually get a fresh instance. Fixed both (see `tracker.ts`/`FilmRoomPage.tsx`) — not reproduced live (needs an actual GPU-context-loss event to trigger), so this is a structural fix for a plausible failure mode, not a verified-fixed one.
34. **(S11) FIXED — occlusion (equipment blocking part of the body) could hijack the focus joint.** `FocusJointPicker.best()` picked whichever joint had the widest apparent range, with no regard for how often it was actually visible — a joint only glimpsed through a gap in equipment can swing wildly (noise, not motion) and rack up a bigger apparent range than the joint that's genuinely, cleanly tracked. Now requires ≥60% visibility across all frames pushed before a joint is eligible, unit-tested.
35. **(S11) FIXED — the first rep(s) of a set were silently dropped.** `RepCounter`/`FocusJointPicker` both need a brief warm-up before they trust anything, and angle samples were only ever pushed to the counter *after* a focus joint existed — so anything that happened during that calibration window (often the very first rep) was fed to nothing. Fixed with a rolling angle buffer + one-time retroactive replay the instant a focus joint is identified (`lib/pose.ts`'s `replayHistory`), unit-tested proving the old approach missed reps the new one catches.
36. **(S11) FIXED — "the videos stop playing together."** Sync-lock only ever re-aligned the reference clip on a transport action (play/step/scrub) — during sustained playback, two independently-playing `<video>` elements are not guaranteed to stay frame-locked and can drift apart within seconds. Added continuous drift correction (re-snap on every `timeupdate` while linked, >0.15s tolerance).
37. **(S11) FIXED — bar path "seemed to be breaking things."** Root cause: pushing a React state update (and recomputing a 600-point SVG polyline) on every single tracked frame — up to 60/sec — was heavy enough to visibly drag down both the tracking loop and video playback. Throttled to ~20 points/sec.
38. **(S11) ADDED — track either clip (client or reference), one at a time.** A `trackTarget` selector lets the coach choose which video the model analyzes; switching restarts analysis against the new clip. Running two *simultaneous* PoseLandmarker instances for true side-by-side comparison with automated angle-delta math is a bigger future step (still open — see debt #12, unchanged) — this delivers the "optional to do one" half of that ask for real, now.
39. **(S11) ADDED — a soft "no person detected" indicator**, not an auto-restart. After ~60 consecutive frames with no landmarks at all while tracking is on, an inline hint names equipment occlusion/out-of-frame explicitly as the likely cause, rather than the skeleton just silently vanishing with no explanation. Deliberately NOT an auto-recovery trigger (unlike debt #33's error-streak one) — a person legitimately stepping out of frame for a moment shouldn't force a restart.
40. **(S11) ADDED — Film Room timestamped notes + natural-language client summary.** Add a note at the current playhead position, click its timestamp to jump back to that moment, and export the whole set's stats + notes as a plain-English block (`lib/filmRoomSummary.ts`, unit-tested) — Copy/Download/Print-stats-sheet always available, "Log to client" needs a client selected and writes into that client's existing Message Log (`messagesRepo`) — it does not use the live E2EE relay from a previous session's messaging work; that's a reasonable future wire-up, not done here.
41. **(S11) DISCOVERED, not built — client progress-report stats sheet already existed.** `PrintProgressReport.tsx` (`/print/progress/:clientId`, linked from `ClientDetailPage.tsx`) already covers sessions/adherence/PRs/e1RM/weekly-volume-chart for a date range — this was built in an untracked prior pass this project doesn't have a session log entry for. Verified it's real and wired up (not just a file that exists — see debt #21's lesson) rather than rebuilding a duplicate from scratch.
42. **(S11) ADDED — a real month-grid Calendar.** The "Calendar" was a chronological agenda list grouped by day — not what most people picture as "a calendar." Added a genuine month grid (`MonthGrid` in `CalendarPage.tsx`: 7-column grid, appointment pills per day, month navigation, click a day to see its full agenda below) as a `Month`/`List` toggle, keeping the original list view rather than removing working functionality. Verified live: creating an appointment, clicking different days, and switching views all work correctly; renders cleanly at a 375px mobile viewport too.
43. **(S11) ADDED — landmark-position smoothing, not just angle smoothing.** The existing One-Euro filter (`OneEuroFilter` in `lib/pose.ts`) only ever smoothed computed joint *angles* via `RepCounter` — the raw `x`/`y` landmark positions that actually draw the on-screen skeleton and feed `barPathPoint` were never filtered, so jitter (and equipment-occlusion noise specifically) showed up directly in what the coach sees. Added `LandmarkSmoother`: runs each landmark through its own One-Euro filter pair, and below a soft `LOW_CONF_VISIBILITY` (0.7) threshold blends the filtered reading toward held history proportional to confidence (fully trusts ≥0.7, fully holds at the hard `MIN_VISIBILITY` gate of 0.5) rather than trusting a low-confidence raw reading outright. Wired into `FilmRoomPage.tsx`'s `onFrame` before angle computation and bar-path point collection. 6 new unit tests (jitter reduction around a stationary held position; recovery-to-a-genuinely-new-position over multiple samples, since a low-pass filter has real lag by design — the first test draft asserted instant convergence and was wrong on its face, fixed by testing convergence over 10 samples instead of 1).
44. **(S11) FIXED — two-phase boot screen, ordered per explicit request ("determinate bar first, then whatever comes after").** `BootScreen.tsx`/`AppRoot.tsx` were a single-phase screen where the determinate bar and the wipe-build reveal weren't sequenced as two distinct, deliberate phases. Rewrote as an explicit `'progress' | 'reveal'` state machine: `AppRoot.tsx` ties real progress values (10/40/55/70/100%) to each actual boot step (trainer load → theme apply → storage.persist → exercise seed), holds the progress phase for a minimum 500ms even on a fast machine so it reads as genuine progress rather than a flicker, then transitions to the reveal phase (mark+wordmark+tagline) for 1300ms before fading out. Deliberately made the bar reflect real progress rather than reusing the brand guide's own decorative infinite-loop demo animation for "determinate" — reasoned that "determinate" implies genuine known-progress by definition. Verified live: DOM-level inspection (not screenshot — the ~2.2s total sequence is faster than this environment's screenshot round-trip) confirmed the `progressbar` element renders alone immediately after a reload, replaced by the wordmark/tagline reveal content several seconds later, matching the intended order; also spot-verified by temporarily stretching both phase durations to 4s, confirming the sequence, then reverting to the original 500ms/1300ms values (checked back into `tsc -b`/`vitest` clean after the revert).
45. **(S11) FIXED — cloud-tier feature gating was inconsistent across surfaces.** `MessagesTab.tsx`'s Live panel checked `!!trainer?.syncServerUrl` inline with no explanation when hidden; separately, `SyncCenterPage.tsx`'s "Cloud Sync" button/`doCloudSync` checked only `trainer?.syncServerUrl` and never looked at `trainer.cloudTier` at all — so a coach who configured self-hosted sync, saved a URL, then switched the tier selector back to "fully local" would still see a live, working Cloud Sync button on the Studio Link page, silently contradicting the "fully local means nothing leaves this device" promise made everywhere else. Added `lib/cloudCapability.ts`'s `cloudCapabilities()` as the single source of truth (tier + configured-URL → `sync`/`messaging`/`reminders` booleans + a human-readable `reasonUnavailable`), 5 unit tests including the exact leftover-URL-after-switching-back-to-local scenario. Rewired `MessagesTab.tsx`'s `LiveMessagePanel` to show two distinct explanatory states (cloud not configured vs. this client not yet paired) instead of returning `null`, and `SyncCenterPage.tsx` to gate `doCloudSync`/the Cloud Sync button on `cap.sync` (which correctly respects `cloudTier`) plus an inline hint on the Cloud Sync Server card when tier is set to local. `messageRelay.ts`'s now-redundant `relayConfigured` export removed (its only caller now uses `cloudCapabilities()` directly). Other cloud-adjacent surfaces (`CloudCard.tsx`) were audited and left as-is — they're the tier *selector* itself, not a feature consumer, so they correctly don't need to ask the capability module.
46. **(S11) DONE — Guide & tutorial content brought current.** The in-app manual (`Guide.tsx`) hadn't been updated for most of this session's additions. Added: a CSV/competitor-roster-import step to "Set up your brand & add clients"; a mention of the client Progress Report stats sheet in "Log sessions & read progress"; a new "Calendar & scheduling" section; Film Room section expanded to cover continuous sync-drift correction, occlusion-robust position smoothing, calibration-window rep replay, timestamped notes, and the Copy/Download/Print-stats-sheet/Send-to-client summary actions; a new "Cloud sync — optional, in three flavors" section describing the 3-tier model and what each tier unlocks; and the closing "Privacy & how it works" section corrected so it no longer unconditionally claims "no server, ever" now that opt-in relay tiers genuinely exist (it now scopes that claim to the default and explains the E2EE guarantee when a relay tier is turned on). Verified live: walked through onboarding on a fresh profile, opened Settings, confirmed every new/changed accordion section renders correctly with zero console errors.
47. **(S11) ADDED — genuine simultaneous dual-clip tracking + a client-vs-reference comparison breakdown, closing the "isn't tracking them side by side" gap.** `trackTarget` (`'A' | 'B'`) was always one-at-a-time; `'both'` is now a real third mode that runs two independent `PoseLandmarker` instances concurrently — not sequentially, and not sharing one instance, because MediaPipe's VIDEO mode is stateful (it uses the previous call as a prior for the next), so alternating frames from two unrelated clips through one shared instance would corrupt its internal tracking rather than just being slower. `tracker.ts` was refactored from module-level singleton state to a `createPoseTracker()` factory so each clip gets its own instance, lifecycle, and timestamp guard; `FilmRoomPage.tsx`'s entire per-clip analysis pipeline (rep counter, focus-joint picker, landmark smoother, angle buffer, bar-path collection, the detection-loop effect itself) was extracted into a reusable `useClipTracking` hook, called once per clip — in single-target modes only one instance is ever active, so behavior there is byte-for-byte unchanged from before (verified live). New `lib/filmCompare.ts` (7 unit tests): `compareReps` matches reps by ordinal (robust even without sync-lock) and reports per-rep depth/tempo deltas; `compareAngles` matches angle-sample histories in lockstep using the same offset "Lock sync" already computes and reports which joint diverges most, on average, only once the clips are actually time-aligned (gated on that, rather than comparing unrelated moments and calling it data). New "Client vs. reference" card + a two-column Movement Analysis layout (Client=verde skeleton, Reference=signal-red skeleton, distinguishable in Overlay mode too) appear only in 'both' mode. **Real resource tradeoff, not free:** two concurrent model instances is measurably more GPU/CPU/memory load than one, on hardware this feature explicitly targets as its floor ("low-tier laptops") — 'both' is opt-in, not the default, for exactly this reason; not yet measured on genuinely low-tier hardware (see new debt below). Verified live with two synthetic canvas-recorded clips fed through the real file-input path: confirmed via network requests that TWO separate `pose_landmarker_lite.task`/`vision_wasm_internal.wasm` loads occurred (proving two real instances, not one reused), both single-target and 'both' modes render correctly with zero console errors, and turning tracking off disposes both cleanly.
48. **(S11) NOT YET MEASURED — dual-tracking's real hardware cost.** Debt #47's simultaneous 'both' mode doubles concurrent MediaPipe inference load. It's opt-in (not default) specifically because of this, and the existing GPU→CPU fallback (debt #29) applies independently to each of the two instances, but no test has been run on hardware that's actually marginal for a single tracker, let alone two at once. If 'both' mode turns out to visibly stutter playback on real low-tier laptops, the next step is either a lower frame-sampling rate specifically for 'both' mode, or a "compare after the fact" alternative that tracks each clip sequentially into its buffer (slower to get results, zero extra concurrent load) instead of true simultaneous tracking.
49. **(S11) NEW — `companion-app/` hand-rolls its own PWA plumbing instead of using `vite-plugin-pwa`.** The latest `vite-plugin-pwa` release caps its Vite peer dependency at `^6`; this project (both the coach app and the new companion app) is on Vite 8, so installing it hit a real `ERESOLVE` conflict, not a preference. Wrote `public/manifest.webmanifest` and a small hand-rolled `public/sw.js` (cache-first app shell, no Workbox) instead — genuinely installable and offline-capable, just without the plugin's automatic precache-manifest generation. Revisit once `vite-plugin-pwa` ships Vite 8 support; the coach app's own long-standing "PWA plugin not yet configured" debt (#4) should probably follow whichever path this one proves out first.
50. ~~(S11) Companion's coach-pairing was UI-only~~ **SUPERSEDED, see debt #52 — real pairing + sync shipped same session.**
51. **(S11) NEW — Companion has no unit tests yet.** Every other `lib/*.ts` pure-logic module in this codebase (coach app and now `filmCompare.ts`) ships with a matching `*.test.ts`, per this project's own convention. `companion-app/`'s sync code (`companionSyncApi.ts`, `PairingFlow.tsx`) is I/O glue (fetch calls, crypto against real keys) rather than pure math, matching why the coach app's own `messageRelay.ts` has no unit tests either — it was verified live instead (see debt #52). The first genuine pure-logic candidate will be whatever merge/reconciliation logic a future pass adds for pulling an assigned program down; write its tests in the same pass that builds it.
52. **(S11) DONE, real sync for Companion — pairing, workout push, and messaging all verified end-to-end against a live server.** Ported `lib/sync.ts` verbatim into `companion-app/src/lib/sync.ts` (same ECDH P-256 + HKDF + AES-GCM, same wire format — duplicated because the two apps are separate npm projects with no shared package, not reinvented). `PairingFlow.tsx` does the real handshake: generates this device's own identity, shows its pairing code, accepts the coach's code, derives the shared key, and requires a spoken safety-number confirmation before saving a non-pending `CoachLink` (mirrors the coach app's own `PairDialog` UX). `companionSyncApi.ts` pushes logged workouts/metrics to `/sync/push` in the coach app's own `SessionLog`/`Metric` shape (tagged `source: 'companion-import'`, a field the coach schema already had for exactly this case) and does full message push/pull against `/messages/*` — the message path is **fully interoperable with the coach app's existing `messageRelay.ts` today, zero coach-side changes needed**, since it mirrors the same request shapes with `direction: 'client'`. **Verified live, not just reasoned about:** ran a real `sync-server` instance locally, generated a synthetic "coach" ECDH keypair in the browser, completed the full pairing handshake (confirmed both sides derive the identical safety number independently), pushed a real logged workout and decrypted it back using the "coach's" key to confirm the exact data round-tripped, sent a message and decrypted that too — all genuine network calls to a running server (confirmed via `read_network_requests` and direct `curl` against the SQLite-backed server), zero console errors throughout. ~~One honest gap remains: the coach app has no code that remaps a pushed client device's placeholder clientId.~~ **CLOSED — see debt #53.**
53. **(S11) DONE — the coach-side clientId remap, AND a file-based sync fallback for fully-local coaches, so all three hosting tiers now use one identical merge path.** Added `remapClientId()` to the coach app's `syncApi.ts`: rewrites a synced client device's placeholder `clientId` (the sending device's own id — a client-side app has no way to know the coach's internal `Client.id`) onto the real one via the `Device.clientId` link made when the coach accepted that pairing — but ONLY for `role: 'client'` devices, never a coach's own second device (which already sends correct ids). Called once, inside `applyPacket()`, which means it applies identically whether the packet arrived over the network (`doCloudSync`) or as a manually-imported `.cwsync` file (`DeviceRow`'s existing "Local Import" — zero new coach-side UI needed for that path, it already existed for coach-to-coach-device sync). 4 new unit tests (`syncApi.test.ts`) covering: remap applied for a linked client device, left alone when not yet linked, never applied to a coach-role device, and rows without a `clientId` field (e.g. exercises) pass through untouched. On the Companion side, added `exportLogsFile()` (`companionSyncApi.ts`) — builds the exact same sealed payload `pushLogsToCoach` would push over the network, but hands back the text for `downloadText()` instead, so a fully-local coach (no relay at all) still gets a real sync path: the client exports a file, sends it any way they like, and the coach drops it onto their existing Local Import button. `CoachCard.tsx` now shows "Sync now" only when a relay URL is on file, and "Export packet" always — the honest, tier-aware UI this needed. **Verified live end-to-end for the fully-local case specifically** (the one path not yet exercised by debt #52's verification): paired with a synthetic coach leaving the relay URL blank, logged a workout, hit "Export packet," captured the real downloaded blob (via a `URL.createObjectURL` intercept, not just checking the button didn't throw), decrypted it with the synthetic coach's key, and independently ran the exact `remapClientId` rewrite on the decrypted data to confirm it produces the correct real `clientId`. `docs/CLIENT_APP_STRATEGY.md` §7 now documents the unified design (one payload shape, one merge function, three transports) plus why this keeps client data manageable/transferable regardless of a coach's tier — a client's own data never leaves their device except as this narrow, purpose-built sync subset, backup/restore and coach-switching are both tier-independent, and unpairing never touches local workout/metric history. 146/146 vitest green (coach app), `tsc -b` clean on both apps.

54a. **(S13, second round) Debt #6's first-boot race was OBSERVED AGAIN live:** a completely fresh browser profile hitting the coach app hung the boot screen at the first step (`trainerRepo.getOrCreate()` never resolved on the initial load; the identical call from the console succeeded moments later, and a reload booted clean into onboarding). This is the strongest live evidence yet that #6 is a real concurrency bug in first-boot getOrCreate (double-mount/racing opens), not a stale-profile artifact — prioritize it in the Phase 9 hardening pass.
56. **(S13) The coach app has NO reminder-scheduling UI.** `sync-server`'s `/reminders/*` endpoints are live and Companion now polls `/reminders/due` on every relay sync (surfacing them as "Reminder: …" messages + notifications) — but nothing coach-side ever calls `/reminders/schedule`, so the pipe is real and empty. The payload contract Companion expects is `{ content }` sealed with the pairing key (same as messages — see `pullReminders` in `companionSyncApi.ts`). Build a small composer (e.g. on MessagesTab or the client Overview) against that.
57. **(S13) Web Push is implemented end-to-end but the FULL round trip needs one real-device pass.** Verified live: relay generates+persists VAPID keys, `/push/vapid`+`/push/subscribe`+`/push/unsubscribe` all work (curl), a coach-direction `/messages/push` fires `pushToClient` and survives dead endpoints (404/410 prunes), and Companion's `enablePush` degrades gracefully when permission is denied (the browser pane hard-denies notification prompts, so grant→subscribe→FCM delivery→sw notification could not be exercised here). Remaining: on a real phone/browser, grant permission, send a coach message, confirm the notification arrives with the app closed. iOS additionally requires the PWA installed to Home Screen (iOS 16.4+) — see CLIENT_APP_STRATEGY.md §9.2.
54. **(S13) The real Electron LAN loop still needs one on-machine, two-device pass.** S13 fixed the LAN sync response path (preload's `ipcRenderer.invoke` → `ipcMain.once` mismatch meant the coach's reply never reached the waiting HTTP response — every Companion WiFi sync would have hung) and verified Companion's `syncOverLan` live against a stub speaking the endpoint's exact contract, plus compiled `electron/` clean. What has NOT run: the actual Electron GUI hosting the server while a phone on the same network syncs against it (folded into T2's existing "verify Windows on a real machine" pass). Until then, treat WiFi sync as "code-complete, contract-verified, GUI-loop-unverified."
55. **(S13) Companion still doesn't poll `/reminders/due`, and `companion/template.html` has none of the app's sync/messaging abilities.** The reminders endpoint is live and verified server-side (S11) but no Companion UI calls it yet; the static HTML export remains file-based only (original T1/T8 text kept in HANDOFF §4 for whoever picks these up). **(S14) Half closed:** Companion polls reminders (done in S13 round 2); the `template.html` half is now a deliberate WON'T-DO — see debt #59.

56. ~~**(S13) The coach app has NO reminder-scheduling UI.**~~ **CLOSED (S14).** `features/sync/reminderRelay.ts` (`scheduleReminder`/`listUpcomingReminders`/`cancelReminder`) + a Scheduled Reminders panel on `MessagesTab`, gated on `cloudCapabilities().reminders` like every other relay feature. Added `DELETE /reminders/:id` to `sync-server` (scoped by `coach_id` in the WHERE clause as well as the ownership assert, and 404s on an already-sent reminder rather than falsely reporting a cancel). **Keying note for anyone touching this:** reminders are keyed by the CLIENT DEVICE id (`Device.id`, which pairing sets to the client's own `deviceId`), because that is what Companion polls `/reminders/due` with — NOT the coach's internal `Client.id`, which the client app has no way to know. Messages use different keying; don't "harmonise" them without reading both sides.

--- new in S14 ---

57b. **(S14) The reminder relay has no scheduler daemon, by design — delivery is pull-only.** A reminder is released the next time the client's Companion app *opens and syncs* after `sendAt`, not at that minute. The UI says so in as many words rather than implying a push notification. Making it minute-accurate on a locked phone needs the Capacitor wrap (same honest limitation as debt #57's Web Push).

58. **(S14) Lighthouse and cross-browser passes were NOT run.** Phase 9's checklist named both. This environment has one Chromium-family browser and no Lighthouse CLI, so running them would have meant either fabricating numbers or reporting a single-engine result as "cross-browser". Everything else in Phase 9 is done and verified; these two need a human with Firefox/Safari and a real Lighthouse run. Nothing is known to be broken — this is unmeasured, not failing.

59. **(S14) DELIBERATE WON'T-DO — no live relay messaging or WiFi sync in `companion/template.html`.** T8/T1 asked for it on the stated premise that "the client's ECDH keypair + the coach's public key already exist from the pairing step — reuse them." **That premise is false for this file:** `companion/export.ts` injects only program/exercise/message data and no keys at all, and the HTML export is not a paired Device. Giving it live sync therefore means either (a) baking a private key into a file that gets emailed and forwarded around — strictly worse than every other crypto decision in this codebase — or (b) reimplementing the app's full pairing handshake in vanilla JS against `file://` localStorage, which is fragile and duplicates the installable Companion app that already does it properly. Chose neither. **What was done instead:** removed the WiFi Sync button, which could never have worked — it POSTed *plaintext* JSON keyed by `Client.id` to an endpoint that requires a *sealed CWSYNC1 packet* from a paired `Device.id`, so it failed at two independent layers and showed the client a misleading "couldn't reach the server" error. Removing it also removed the `html5-qrcode` **CDN `<script>`**, which was a genuine offline-doctrine and privacy violation in a file meant to work with no connection. The file-export path (which works, and drops into the coach's existing Local Import) is now the single clear call to action. **Revisit only if** the HTML export ever needs live sync badly enough to justify on-device key generation there.

60. **(S14) `singleFlight` is duplicated in both apps' `lib/core.ts`**, like `lib/sync.ts` and `lib/pose.ts` before it. Same reason: two separate npm projects, no shared package. If a third copy ever appears, that is the signal to introduce a real shared workspace rather than a fourth.

61. **(S14) Companion's Film Room has not been tested on real human footage** — the same gap the coach app carries as debts #7/#10. The pipeline is verified end to end (model loads from the app's own bundle, ~360 frames processed, zero exceptions, no-person hint fires), and the analysis math is byte-identical to the coach app's unit-tested `lib/pose.ts`. What is unverified here is only rep counting against a real person, and specifically **how any of it performs on an actual mid-range phone** — every measurement so far is desktop. Needs one pass with a real phone and a real set.

64. **(S15, updated) i18n: the layer and RTL are DONE; the string conversion is not.** ~~RTL layout pass has not happened~~ **CLOSED** — ~130 physical CSS classes are now logical properties across 37 files, and RTL was verified live in Arabic (sidebar flips, no overflow on 8 routes). **What remains is purely mechanical:** roughly **53 of 57 components still hold hardcoded English strings**. The pattern is proven and safe — `useTranslation()`, move the string into `locales/en.ts`, and the compile-checked key union means a typo can't reach runtime. Two standing cautions: **(a) when converting, keep using logical properties** (`ms-`/`me-`/`ps-`/`pe-`/`start-`/`end-`) so RTL doesn't regress; **(b) `es.json` and `ar.json` are SEED translations**, each marked in its own `_meta` key, and must not ship to customers as finished locales.

64. **(S15) i18n is wired but only ~5% converted.** The layer works end-to-end (verified live in Spanish) and `AppRoot`/`Shell` are done, but **roughly 65 components still hold hardcoded English strings**. This is mechanical, not hard — `useTranslation()`, move the string into `locales/en.ts`, done — and the compile-checked key union means a typo can't slip through. Two things to know before continuing: (a) **the RTL layout pass has NOT happened** — `isRtl()` and the `dir` attribute are unit-tested and wired, but no Arabic catalogue exists yet, so nothing has actually been *seen* in RTL; Film Room's dual-video stage and the sidebar are the likely trouble spots, and CSS logical properties (`margin-inline-start`, not `margin-left`) should be adopted while converting rather than retrofitted after. (b) **`es.json` is a seed translation, not a reviewed one** — it is marked as such in its own `_meta` key and must not ship to customers as finished Spanish.

63. **(S15) `daysSince()` can be off by one across a timezone boundary.** Noticed in Quick Log's client card, which read "Last session 1d ago" for a session logged today: the helper compares a `yyyy-MM-dd` string (parsed as UTC midnight) against local `Date.now()`, so it rounds up when local time is behind UTC. Cosmetic and **pre-existing** — the sidebar backup-health indicator uses the same helper — but it should be fixed once, in `lib/core.ts`, rather than worked around per call site.

62. **(S14) The service worker is cache-first with background revalidate**, so the first load after an update can serve the previous build and pick the new one up on the following load. Standard for this strategy and acceptable for an offline-first app, but it means "reload twice" is the honest answer if a coach reports not seeing a fix. Bump `CACHE` in `public/sw.js` on any release where that matters.

## Session log
- **S1 (Fable):** Phases 0–1 complete + gates. Phase 2 core. Seed architecture + 48 exercises. All docs. Handoff prepared → `HANDOFF_SONNET.md`.
- **S2 (Antigravity):** Completed Phase 2 (EditClientDialog, NotesTab v2 schema, DataCard hard-delete, Restore dialog). Completed Phase 3 (Expanded seed exercises, fuzzy matcher `src/lib/fuzzy.ts`, `LibraryPage.tsx` with dense Table + filter chips + ExerciseDetailDialog). Build & tests passed.
- **S3 (Antigravity):** Completed Phase 4 (Program Builder). Built ProgramsPage, ProgramBuilder with robust history stack (undo/redo), BuilderOutline, DayCanvas using `dnd-kit` for vertical drag-and-drop, ExerciseRow for spreadsheet-style set editing, and ExerciseSearch with fuzzy lookup. Build and vitest (12/12) pass clean.
- **S4 (Antigravity):** Completed Phases 5–7 (Logging, Analytics, Companion). SessionLoggerPage, OverviewTab with SVG charts, MetricsTab, Companion template + export + .ssdata import with mergeUpsert. Build and vitest (12/12) pass clean.
- **S5 (Antigravity):** Completed Phase 8 (Operations). CheckInsTab, BillingTab, CalendarPage, BusinessPage, ReportsPage, CommandPalette, OnboardingWizard. All placeholder pages replaced with real implementations. Build and vitest (12/12) pass clean.
- **S6 (Fable):** v1.1 differentiators, hardest-first. (1) **Film Room** `features/filmroom/FilmRoomPage.tsx` + route + nav: local-only video comparison (side-by-side/overlay+blend), frame-step transport w/ selectable fps, sync-lock offset, SVG line+angle annotation tools with pixel-true angle math. (2) **Progression engine** `lib/progression.ts` (linear-load / double-progression / rpe-target + policy-free heuristic, every suggestion carries a reasoning line) surfaced as "Suggested next" in ExerciseHistoryDrawer. (3) **Profit Planner**: `Expense` type, Dexie v3 (`expenses` table), envelope schemaVersion 1→2, `expensesRepo`, `Trainer.monthlyProfitTarget`, pure math in `lib/business.ts`, planner+expenses UI in BusinessPage. (4) 12 new tests (`lib/progression.test.ts`) → **vitest 24/24 green; `npm run build` clean** (657KB/199KB gz). Browser-verified: schema v3 migrates, goal persists, expense CRUD recomputes gap/projection live; Film Room renders. Spec §3 table + new §4.16–4.19 written; `BRANDING_PLAN.md` created at repo root. Debts 6–9 logged honestly.
- **S7 (Fable):** v1.2 intelligence layer, hardest-first. (1) **Movement tracking AI**: `npm i @mediapipe/tasks-vision`; wasm runtime + pose_landmarker_lite model bundled into `public/mediapipe/` (offline, zero API keys); `lib/pose.ts` pure analysis (joint angles w/ visibility gate, RepCounter hysteresis, FocusJointPicker, depth/symmetry %, BONES) + `filmroom/tracker.ts` lazy wrapper (own 132KB chunk); FilmRoomPage: Track-movement toggle, skeleton overlay w/ letterbox mapping, rVFC-driven loop w/ timeupdate fallback, Movement-analysis card. (2) **Nutrition engine** `lib/nutrition.ts` (Mifflin-St Jeor→TDEE→goal kcal→macros/fiber/water, every number with cited rationale; + warmupRamp) + `NutritionTab.tsx` wired into ClientDetailPage; Client gains sex/heightCm/birthDate/activityLevel/nutritionGoal (unindexed, no schema bump). (3) **Readiness** `lib/readiness.ts` + card on CheckInsTab (1–10 scales matched to the form). (4) **Gym cut** `Client.gymCut` + GymCutCard on BillingTab + `gymCutForClient/ForMonth` in `lib/business.ts`, subtracted in profitPlan + projection, ember stat on Business page. (5) 14 new tests (`lib/engines.test.ts`) → **38/38 green, build clean** (main 663KB + lazy vision 132KB). Browser-verified end-to-end: nutrition plan renders w/ correct BMR/macros+citations, readiness 53/Amber w/ drivers, gym cut −$150 of $500@30% flows to net, tracker init loads all 3 MediaPipe files from local origin (zero external requests) and playback loop runs clean. Fixed S6 misplacement: npm artifacts accidentally created at repo root removed; mediapipe assets moved into the project. Debts 10–12 added.
- **S8 (Fable):** (1) **Rename Strongsuit → Coachwright** (Strongsuit trademarked by a ~$10M company). Created `src/lib/brand.ts` as the single source; repointed Shell, index.html, onboarding, dashboard, companion footer, tracker, Settings, package.json. Data-safe: kept IndexedDB name + added `coachwright` backup `app` id while still accepting legacy `strongsuit`; encrypted-backup magic `CWENC1` with `SSENC1` fallback; `.coachwright`+`.strongsuit` both restorable. (2) **Film Room dual controls**: new `useClip` controller hook + `TransportBar`; side-by-side gives Client and Reference each an independent transport; Lock-sync (or overlay) collapses to one master "Both — synced" bar; **Flip client/ref** horizontal mirror (completed the prior session's scaffolded `mirrored`/`footer` props; removed the dead `MiniTransport`). (3) **In-app Guide & tutorial** (`features/settings/Guide.tsx`) — 11 accordion sections covering the whole app incl. a full Film Room walkthrough, in brand voice. (4) **39/39 vitest green, build clean.** Browser-verified: title/sidebar/guide all read Coachwright, pre-rename seeded client survived (proves data compat), Reference plays while Client stays paused (independent), Lock-sync → single master bar, tracking still initializes from local mediapipe files only (zero external requests), zero console errors. Docs + BRANDING_PLAN renamed; debts 13–15 added.
- **S9 (unidentified session — reconstructed by S10, did not update this ledger):** Built EULA gate, `CoachMessage`/`MessagesTab`, `PrintSessionSheet`, a full Electron desktop shell (`electron/`), device-pairing sync UI (`SyncCenterPage`, `WiFiSyncDialog`), and a standalone `sync-server/` cloud-relay prototype. Schema advanced to v5 (messages) then further to v6 by S10. Real, mostly-working code, but shipped with several bugs (see S10 entry) and **zero doc updates** — reconstructed here so the relay isn't broken for whoever's next.
- **S10 (Fable):** Opened by auditing S9's undocumented work: ran `tsc -b`/`vitest`/`build`, found and fixed real bugs — `tsconfig.node.json`'s `module: nodenext` broke `vite.config.ts` typecheck (reverted to `ESNext` + explicit `moduleResolution: bundler`), `Shell.tsx`'s loading guard didn't cover the `undefined` case after `trainerRepo.get()` replaced `getOrCreate()` (now `if (!trainer)`), `WiFiSyncDialog` threw when `window.electronAPI` was absent in the web build (added a typed guard + a "requires desktop app" empty state), and five components used non-existent Tailwind classes (`bg-brand`/`text-brand-500`/`hover:bg-ember-50` etc.) that silently no-op — replaced with real tokens throughout. Verified the whole S9 surface live in-browser (EULA gate, Coaching tab, recurring Calendar, Sync Center, Messages) — zero console errors. **Then built the "gym-management-suite" feature list**, tiered honestly: everything that fits zero-backend was built for real (Team & locations + commissions, CRM/leads pipeline, leaderboards/challenges, progress photos, habit tracking w/ streaks, invoicing/coupons/account-balances, a configurable local **Automations rule engine** replacing the hardcoded 7-day Dashboard check, TV Workout full-screen mode); everything that fundamentally needs a server (payments processing, bulk email/SMS, push notifications, sales funnels, on-demand video/livestreaming, storefronts) was **documented, not faked**, in new `docs/SERVER_STRATEGY.md` with concrete bring-your-own-account patterns (Stripe Payment Links, mailto:/sms: deep links) — two of which were actually wired in (a "Pay now" payment-link button on invoices, mailto:/sms: quick-contact links on the client header). Windows: verified Electron compiles clean and `dist/` uses `file://`-safe relative paths (a live GUI launch/installer build wasn't executed in this sandbox — see debt #18). Android: chose Capacitor over React Native/Cordova (documented why in `docs/ANDROID_STRATEGY.md`), installed it, and ran `npx cap add android` — a real, generated `android/` Gradle project exists but has **never been built or run** (no SDK in this environment — debt #17). New engines: `lib/business.ts` (+staff commissions, coupon/invoice math), `lib/automations.ts`, `lib/leaderboard.ts`, `lib/habits.ts`, `lib/media.ts` — 15 new tests in `lib/enterprise.test.ts`, all pure and unit-tested. Caught one real bug through actual browser verification (not just code review): TV Workout's reps/time display had a `??`/`?:` operator-precedence bug rendering "2×undefineds" — fixed and re-verified. **75/75 vitest green, `npm run build` clean** (main ~820KB/~247KB gz + lazy 135KB MediaPipe chunk). Debts 16–21 added. Docs: this file, `HANDOFF_SONNET.md`, spec §3/new §4.24–4.30, `docs/SERVER_STRATEGY.md`, `docs/ANDROID_STRATEGY.md` all written/updated this session.

- **S11 (Claude Sonnet 5):** Picked up a very large multi-part user ask mid-way through (this session started with a prior Sonnet instance that ran out of context — its work is folded into the "v1.6" row above rather than split into two entries, since it's one continuous session's output). Shipped, in priority order: feature-visibility toggles + 3-tier cloud hosting Settings UI, robust multi-link in-app exercise video, a rest timer, biomechanics consistency/bar-path tracking, nutrition carb-cycling/diet-break advice, research-backed metric presets, client/coach data portability (`db/portability.ts`), competitor CSV roster import (`lib/csv.ts`), a real logomark + boot screen (replacing a leftover generic favicon and closing a real IndexedDB race in `main.tsx`), and a Film Room bug fix (tracker timeline not resetting on mid-session clip replacement). Ran a live empirical test of Film Room's tracking pipeline (synthetic canvas video fed through the actual file input via a scripted `DataTransfer`, not just code review) — confirmed the MediaPipe init/detection loop itself runs clean (all 3 asset files 200, "Graph successfully started running", zero console errors) before hunting for the more subtle clip-replacement bug via code reading. Also swept ~10 more files for the recurring undefined-Tailwind-class bug (debt #20) and found two components referencing an animation/shadow class that was never defined at all — fixed by adding the real definitions to `tailwind.config.js`. Told to keep going, then built out `sync-server/server.ts`'s messaging (`/messages/push`/`/pull`) + poll-based reminders (`/reminders/schedule`/`/due`/`/upcoming`) + per-coach API keys (`/keys/register`/`/revoke`, admin-gated) — **verified every endpoint live** by running the server with `npx tsx` and curling each one (message directionality both ways, cross-tenant 403 on a mismatched coachId, reminder due→already-sent transition, legacy shared-key back-compat), not just by reading the code; wired the coach-side client (`features/sync/messageRelay.ts`, reusing `lib/sync.ts`'s existing E2EE pairing crypto rather than inventing a new scheme, plus a "Live" send/pull panel in `MessagesTab.tsx`); rewrote `docs/SERVER_STRATEGY.md` (new §2.5) to formalize the 3-tier hosting model as doctrine instead of speculation. **37 new tests, 112/112 vitest green, `tsc -b` clean, `npm run build` clean.** Did NOT get to: the Companion (client-facing) side of messaging/reminders — `companion/template.html` is vanilla JS with no framework and has no code yet to push a reply or poll for reminders (deliberately left for a dedicated pass rather than rushed, since it can't be typechecked/unit-tested the way the rest of this session's work was) — nor a responsive/dynamic-sizing pass or Electron polish. Debts #22–26 added (#22 closed same session); `HANDOFF_SONNET.md` §4 re-pointed at these as the next task queue, in order. **Continued, same session — full branding pass:** Caleb supplied a real brand-mark reference (`Coachwright Logo.dc.html`, a standalone identity sheet) and had already hand-applied its core pieces to three files (`Logomark.tsx`, `favicon.svg`, `BootScreen.tsx` — a barbell-collar mark that resolves to a hard "C" in negative space, display font switched Archivo→**Inter Tight**, `cw-wipe`/`cw-word`/`cw-fade`/`cw-spin`/`cw-bar` motion keyframes registered in `tailwind.config.js`) before asking to finish the rest. Audited the whole app for anything still on the old identity: found and fixed **`electron/splash.html`** — a fully generic, off-brand splash (bouncy elastic easing, gradient-green circle, plain "C" letter, gradient text) that turned out to be **dead code** (`splashWindow` was declared in `electron/main.ts` but never instantiated or shown — the file never actually displayed). Rewrote it to match `BootScreen.tsx`'s exact treatment (same mark, same `cw-wipe`/`cw-word`/`cw-fade` sequence) and **wired it in for real** (`createWindow()` now shows the splash immediately, holds `mainWindow` hidden until `ready-to-show`, then swaps). Also fixed: `PrintSessionSheet.tsx` had "Coachwright" hardcoded as a literal string instead of importing `APP_NAME` from `lib/brand.ts` (the one rule this project enforces above almost everything else) — fixed, and added the mark to the printed header. Removed the now-dead `@fontsource/archivo` dependency from `package.json` (confirmed zero imports anywhere first). Updated both brand docs to match what's actually shipped: `BRANDING_PLAN.md` §4 (mark is no longer "optional," typography is Inter Tight not Archivo, added a new motion-doctrine paragraph) and `STRONGSUIT_MASTER_SPEC.md` §7.2/§7.4b (same font correction, new §7.4b documents the mark + motion system as an official signature element). Verified visually in-browser (not just by reading code): onboarding step 1, "Your Identity" step, Data Ownership step, EULA screen, and the main Shell sidebar all render the new mark + Inter Tight correctly; confirmed dark mode doesn't invert the mark's badge (it's deliberately not theme-tokenized, by design); confirmed the Electron splash renders standalone exactly as designed (screenshotted `splash.html` directly). `tsc -b` clean on both the main app and `electron/tsconfig.json`, 112/112 vitest still green, `npm run build` clean, zero console errors. Confirmed `public/icons.svg` (an unrelated orphaned social-icon sprite from the original scaffold) is unreferenced anywhere — logged as debt #27, not touched, out of scope for a branding pass. **Continued again, same session — Caleb pushed back that the sidebar mark still wasn't what he wanted and asked for the full variant set + a picker.** Rebuilt `Logomark.tsx` into a proper family matching every swatch in the reference sheet: `Logomark` (square badge), `Wordmark`, `MonogramCW`, `HorizontalLockup`, `Lockup` (full hero treatment), plus a `BrandMark` dispatcher and `BRAND_MARK_VARIANTS` registry. Two real corrections came out of rebuilding this properly: (1) the mark had been rendered oversized relative to the badge (~77% fill) versus the reference sheet's own ~60% ratio across every swatch — recomputed the transform to center-and-scale correctly (`translate(24 24) scale(0.343) translate(-44.4 -50)`, derived from the path's actual bounding box, not eyeballed); (2) **the badge's fixed dark tone would have nearly disappeared in dark mode** — ink `#171A1E` against the dark theme's own surface color `#171B20` is a ~1-in-16-million-shades-apart contrast failure — so `Logomark` now auto-swaps ink/porcelain tone via Tailwind's `dark:` variant by default (two stacked SVGs, one hidden per theme), with an explicit `tone` prop to pin one variant for fixed-background contexts (`PrintSessionSheet.tsx`'s always-white paper now pins `tone="dark"` explicitly). Added `Trainer.sidebarLogoVariant` (types.ts, no schema bump) + a new `BrandMarkCard` in Settings showing all 5 variants as live-previewed, clickable swatches; `Shell.tsx` now renders whichever the coach picked (both the desktop sidebar and the mobile header), with `animated` wired on true since `Shell` never remounts across navigation (plays once per session, holds its end state — confirmed safe, not a replay risk). Also simplified `electron/splash.html`'s mark container (it had an invisible same-color-as-page "badge" box) and gave `favicon.svg`/`index.html`'s static pre-boot splash the same corrected ratio + sharp corners (`index.html`'s version also now media-query-swaps tone with `prefers-color-scheme`, mirroring the same dark-mode-contrast fix). **Then moved to "fix all bugs in the Film Room."** Hardened `tracker.ts`'s `initTracker()` with a CPU-delegate fallback — it only ever tried `delegate: 'GPU'` before, with no retry path, on hardware this feature explicitly targets ("low-tier laptops," per the file's own header comment) where WebGL2 can be unavailable or blocklisted; now tries GPU then CPU before surfacing an error, and the error copy no longer falsely blames "missing model files" for what could be a delegate-creation failure. Did a second full live-interaction pass on Film Room (synthetic dual clips, both loaded via the real file-input path): overlay mode, mirror flip, the Line and Angle annotation tools (all 2-/3-point draws, verified the rendered SVG geometry and the computed angle — 124° — were exactly right), and sync-lock (verified `alignB()` re-snaps the reference clip to the locked offset on every real transport action) **all work correctly** — several looked "broken" on the first attempt but every one turned out to be a testing-harness artifact (browser-pane click-coordinate scaling mismatch, React's `onChange` on `<input type=range>` actually listening for the native `input` event not `change`, dispatching two clicks synchronously with no tick for React to flush state between them) rather than a real app bug, confirmed by retrying each one through a reliable path (direct `.click()` calls, or real delays between dispatched events). **Found one genuine, unrelated, and significant bug along the way** while spot-checking the Program Builder at a mobile viewport (which is where "continue to expand… as we planned" led next, since a responsive pass was already visibly mid-flight in `Shell.tsx`): `DayCanvas.tsx`'s empty-day "Add Exercise" button called `addBlock()` then immediately read `day.blocks.length` from the same render's stale `day` prop to decide what to pass to `setTargetBlockId` — since `updateDay`→`commitChange` doesn't update `day` until the *next* render, this always evaluated to `day.blocks.length > 0 → false` on a truly empty day, so `targetBlockId` was set to `null`, and `handleAddExercise`'s `if (!targetBlockId) return` then silently no-op'd every time — **the single most common first action in the entire Program Builder (new day → add first exercise) silently failed for every user, desktop and mobile alike.** Fixed by building the new block with a known id up front and targeting it directly, no stale read. Verified broken-then-fixed with a live before/after (added a fresh Day 2, confirmed selecting an exercise did nothing pre-fix, rebuilt, reran the identical steps, confirmed Back Squat + its set-editor row appeared correctly). While in there, confirmed the mobile-responsive pass already underway in `Shell.tsx` (drawer nav, mobile header, hamburger toggle) extends further than expected: Dashboard, Clients (empty state + roster table via `Table`'s existing `overflow-x-auto`), Programs list, and — now that the bug above is fixed — the Program Builder's outline/day-canvas/exercise-row stack all render and function correctly at a 375px viewport, verified live, not assumed. `tsc -b` clean (app + electron), 112/112 vitest, `npm run build` clean throughout every step above.

**Continued once more, same session — Caleb reported "tracking sometimes breaks," the first-reps-ignored issue, and that the Full lockup didn't look designed.** Fixed all three (see debts #30–32 for the full writeup): `tracker.ts` now distinguishes a real `detectForVideo` exception from a clean "no person" miss and auto-recovers (dispose+reinit) after 12 consecutive errors, and `toggleTracking`'s off-branch now actually disposes the cached tracker so switching it back on gets a genuinely fresh instance instead of a possibly-wedged one. `FilmRoomPage` now buffers every frame's angles from the start of tracking and replays the whole buffer through `repCounter` the instant a focus joint is first identified (new `lib/pose.ts` export `replayHistory`, unit-tested with a synthetic history proving the old approach missed reps 1–2 of a 3-rep series and the new one catches all three). Rebuilt `Lockup` in `Logomark.tsx` — it had been a vertically-stacked arrangement of my own invention; the reference sheet's actual "Primary Lockup" is a horizontal row (icon left, wordmark+tagline right), rebuilt to match its exact proportions (badge:gap:wordmark:tagline ≈ 132:40:59:13px from the sheet's own numbers) and re-verified visually in both the Settings picker and the live sidebar. 113/113 vitest (1 new test), `tsc -b` clean, `npm run build` clean.

**Continued yet again, same session — a large follow-up covering QOL, Film Room reliability, and a real calendar.** Fixed the Full lockup's fit properly this time (tagline still wrapped to two lines at sidebar scale — shrunk its font/tracking further, tightened both gaps, `markSize` 40→34; debt #32). Film Room, in priority order the user reported them: (1) hardened `FocusJointPicker` against equipment occlusion — it picked the joint with the widest apparent range with no regard for how often that joint was actually visible, so a limb only glimpsed through a gap (noisy, not real motion) could out-rank a genuinely well-tracked one; now requires ≥60% visibility across frames, unit-tested with a synthetic occlusion scenario (debt #34). (2) fixed the "first reps always ignored" issue for real this time at its true root — angle samples were never pushed to `RepCounter` until a focus joint existed, so anything that happened during that calibration window was fed to nothing; added a rolling angle buffer + one-time retroactive replay via `lib/pose.ts`'s new `replayHistory`, unit-tested (debt #35). (3) fixed "videos stop playing together" — sync-lock only re-aligned on transport actions, not during sustained playback, so two independently-decoding `<video>` elements drift apart within seconds; added continuous `timeupdate`-driven drift correction (debt #36). (4) fixed bar path "breaking things" — it was pushing a React state update + recomputing a 600-point SVG polyline on every single tracked frame (up to 60/sec); throttled to ~20/sec (debt #37). (5) added a `trackTarget` selector so the coach can track either the client or reference clip (not true simultaneous dual-tracking with automated comparison — see debt #12, still open, this delivers the "optional to do one" half honestly; debt #38). (6) added a soft "no person detected" hint (not an auto-restart) naming equipment occlusion/out-of-frame explicitly after ~60 consecutive miss-frames, distinct from the error-streak auto-recovery already built (debt #39). (7) added Film Room timestamped notes (click a note's time to jump back) + a natural-language client summary generator (`lib/filmRoomSummary.ts`, unit-tested, escapes HTML for the print path) with Copy/Download/Print-stats-sheet/Log-to-client actions (debt #40). While building the "stats sheet on client profiles" ask, **discovered `PrintProgressReport.tsx` already existed and was already linked from `ClientDetailPage.tsx`** — verified it was real and wired up (not just a file sitting there unreferenced) rather than building a duplicate from scratch (debt #41). Finally, replaced the Calendar's chronological-agenda-list-only view with a real month grid (`MonthGrid` component: 7-column grid, appointment pills, month nav, click-a-day-for-its-agenda) behind a Month/List toggle, keeping the working list view rather than discarding it — verified live (created an appointment, clicked between days, switched views, all correct) and at a 375px mobile viewport (debt #42). **12 new tests** (`engines.test.ts` occlusion + replay-history cases, `filmRoomSummary.test.ts` ×10 covering both the plain-text and HTML-escaping paths) → **124/124 vitest green**, `tsc -b` clean, `npm run build` clean throughout every step. Did not get to a broader mobile-friendliness audit beyond Calendar (Film Room's dual-video stage, Business, Settings still unverified at mobile width — same gap as debt #24) — flagged, not silently skipped.

**Continued once more, same session — asked to make tracking "more advanced," sequence the boot screen (determinate bar first, then the reveal), reason explicitly about what does/doesn't work without the cloud, and bring the Guide/about content current.** (1) Built `LandmarkSmoother` (`lib/pose.ts`) — extends the existing One-Euro filtering (previously applied only to computed joint angles) to raw landmark *positions*, with visibility-weighted blending toward held history below a 0.7 soft-confidence threshold; wired into `FilmRoomPage.tsx`'s frame loop ahead of both angle computation and bar-path point collection. 6 new tests; the first test drafts asserted things a low-pass filter can't honestly do (tracking a moving trend with zero lag, converging to a new position in one sample) and were rewritten to test what the filter is actually supposed to do (jitter reduction around a held position; multi-sample convergence after a genuine change) rather than loosening the implementation to fit a wrong assertion. (2) Rewrote the boot sequence into an explicit two-phase state machine (`AppRoot.tsx`/`BootScreen.tsx`): a determinate bar tied to real boot-step progress plays first, held for a minimum 500ms so it reads as genuine even on a fast machine, then the wipe-build mark/wordmark/tagline reveal plays second for 1300ms before fading out — deliberately real progress rather than reusing the brand guide's own decorative infinite-loop demo for "determinate." Verified live via DOM inspection immediately after a reload (caught the bare `progressbar` element, then the reveal content several seconds later, in the right order) since the ~2.2s total sequence is faster than this environment's screenshot round-trip; cross-checked by temporarily stretching both phase durations to 4s to confirm visually via the accessibility tree, then reverting and re-verifying `tsc -b`/`vitest` clean. (3) Built `lib/cloudCapability.ts` as the single source of truth for what a coach's hosting tier unlocks (5 unit tests), and while wiring it into `MessagesTab.tsx`'s Live panel, found and fixed a real, separate inconsistency in `SyncCenterPage.tsx`: its Cloud Sync button only ever checked for a saved `syncServerUrl` and never looked at `cloudTier` at all, so a coach who saved a URL under self-hosted and then switched back to "fully local" would still have a working Cloud Sync button — silently breaking the "fully local means nothing leaves this device" promise the rest of the app makes. Fixed by gating on `cloudCapabilities().sync` instead, plus an inline hint on the Sync Center's server-config card explaining why a saved URL isn't being used while the tier is set to local. Removed `messageRelay.ts`'s now-redundant `relayConfigured` export. (4) Brought `Guide.tsx` current with everything shipped this session that it didn't yet mention: CSV import, the client Progress Report stats sheet, a new Calendar section, Film Room's reliability/notes/export additions, a new "Cloud sync — optional, in three flavors" section, and a corrected Privacy section that no longer unconditionally claims "no server, ever." Verified live end-to-end: walked a fresh profile through onboarding (confirmed the onboarding's own "Data Ownership" step copy already matched the new 3-tier framing independently, a good consistency sign), opened Settings, and read through every new/changed Guide section in the live accessibility tree with zero console errors. **135/135 vitest green** (11 new tests this leg: 6 `LandmarkSmoother`, 5 `cloudCapability`), `tsc -b` clean, full suite re-run clean after every edit including the two SyncCenterPage narrowing fixes TypeScript caught (`trainer` used inside nested function declarations after an early-return guard doesn't narrow the same way a top-level check does — resolved with optional chaining, same pattern as the existing `MessagesTab.tsx` code). Debts #43–46 added.

**Continued once more, same session — Caleb reported Film Room tracking "still isn't good enough" and specifically that it "isn't tracking them side by side and giving advanced breakdowns," then asked for client-facing mobile app groundwork: a second app for the people being coached, local-first like the coach app, syncing over the cloud when they have a coach, with its own paid tier for a client who wants cloud features without one.** (1) **Real simultaneous dual-clip tracking** — see debt #47 for the full writeup: `tracker.ts` refactored from a module-level singleton to a `createPoseTracker()` factory, `FilmRoomPage.tsx`'s entire tracking pipeline extracted into a `useClipTracking` hook called once per clip, a new `trackTarget === 'both'` mode runs two independent MediaPipe instances at the same time (verified live via network requests — two separate model/wasm loads, not one reused), and a new `lib/filmCompare.ts` (7 tests) delivers the actual "advanced breakdown": rep-by-rep depth/tempo deltas plus per-joint angle deviation once the clips are time-aligned. Single-target modes are unchanged (verified live, byte-for-byte same UI). (2) **`docs/CLIENT_APP_STRATEGY.md`** — a full architecture and pricing doc for **Companion**, a second, client-facing app: local-first Dexie DB per client, an optional `CoachLink` (reusing the existing pairing/ECDH crypto, not a new scheme) for clients who have a coach, and a client-only "Personal Cloud" tier ($3.99/mo or $29/yr) for a client who wants cross-device sync of their *own* data without needing a coach at all. Spells out the exact sync mechanics precisely because "it syncs to the cloud" is meaningless without saying whose cloud: client↔coach data ALWAYS transits the *coach's* relay (free to the client, the same messaging/reminder endpoints already shipped this session), never the client's Personal Cloud subscription — the two are independent and the doc calls that out explicitly so it doesn't get conflated in UI copy later. (3) **Scaffolded `companion-app/`** as a real, separate sibling Vite+React+Dexie project (not a route bolted onto the coach app) — its own package.json/tsconfig/tailwind config (same design tokens, so the brand feels the same across both apps), a hand-rolled PWA manifest + service worker (`vite-plugin-pwa` doesn't support Vite 8 yet — real `ERESOLVE` conflict, not a choice; debt #49), a bottom-tab-nav Shell (Home/Log/Progress/Settings), an onboarding flow (name → "I have a coach" vs. "I'm training myself" → pairing-code capture, honestly stored as `pending` since the real sync wiring isn't built yet — debt #50), full CRUD for standalone workout logging and body-metric tracking, and a Settings page with the Personal Cloud pricing card (`Coming soon`, disabled, no fake purchase flow) plus a real JSON backup/restore round trip. `npm install` + `tsc -b` clean; verified every screen live in the browser (onboarding through both branches, logging a real workout — confirmed it persisted to Dexie and rendered back — Progress and Settings screens, zero console errors throughout). Companion has no unit tests yet because it has no pure-logic modules yet (debt #51) — everything so far is UI + direct repo calls, which is honest for a v0 scaffold, not a gap. Debts #47–51 added; this doc's own §6 build order (steps 3 and 5) is the explicit next-task queue for this new product line.

**Continued once more, same session — asked to "continue to make it full fledged," and specifically to think through how a client's cloud data actually reaches a coach.** Took that literally: made Companion's coach-pairing genuinely real instead of the honest stub from the previous leg (build-order step 3). Ported `lib/sync.ts` verbatim into `companion-app/src/lib/sync.ts` (same ECDH P-256 + HKDF-SHA256 + AES-GCM, same wire format — a second copy since the two apps share no package, not a reinvention). Built `PairingFlow.tsx`: generates this device's own identity on first use, shows its pairing code, accepts the coach's code, derives the shared key, and requires the same spoken-safety-number confirmation the coach app's own `PairDialog` requires before saving a real (non-pending) `CoachLink`. Built `companionSyncApi.ts`: `pushLogsToCoach` seals logged workouts/metrics into the coach app's own `SessionLog`/`Metric` shape (tagged `source: 'companion-import'` — a field the coach schema already had, built for exactly this) and pushes via `/sync/push`; `pushMessageToCoach`/`pullMessagesFromCoach` do the same for `/messages/*`, mirroring the coach app's existing `messageRelay.ts` request shapes exactly (`direction: 'client'` instead of `'coach'`) — this half is **fully interoperable today, zero coach-side changes needed**, since the coach app's `pullRelayMessages` already knows how to read what this pushes. Wired a `CoachCard.tsx` in Settings (real pairing entry point for solo clients who add a coach later, a "Sync now" button, a message thread) and rewired onboarding's "pair" step to use the same flow. **Verified end-to-end against a real, running `sync-server` instance** — not just typechecked: started the actual Express+SQLite server locally, generated a synthetic "coach" ECDH keypair in the browser console, completed the full pairing handshake through the real UI (confirmed both sides independently derive the identical safety number — 742930 — proving the crypto is genuinely symmetric, not just displaying a number), logged a real workout, hit "Sync now," and confirmed via `read_network_requests` that real `POST /sync/push` and `GET /messages/pull` calls succeeded against the live server; then, using the synthetic coach's private key, decrypted the pushed payload straight from the server's own storage and got back the exact workout data (exercise name, reps, everything) that was logged in the app — then did the same for a sent message. Zero console errors throughout. One honest gap flagged, not hidden: pushed `SessionLog` rows carry this device's own `deviceId` as a placeholder `clientId`, since Companion has no way to know the coach's internal `Client.id` — a coach-side addition (look up `Device.clientId` at ingestion) closes that, tracked as `HANDOFF_SONNET.md` T12's remaining half. `tsc -b` clean (both apps), coach app's 142/142 vitest still green (untouched). Debt #50 superseded by #52 (full writeup); #51 updated to explain why this sync code has no unit tests (I/O glue verified live, same convention as the coach app's own `messageRelay.ts`).

**Continued once more, same session — asked to close the remaining gaps, and specifically to design (not just build) how client data stays manageable/transferable and how coach sync works identically regardless of whether the coach is on their own cloud, our cloud, or no cloud at all.** Closed the one gap flagged at the end of the previous leg: added `remapClientId()` to the coach app's `syncApi.ts` (pure function, 4 new unit tests), called once inside `applyPacket()` — rewrites a client device's placeholder `clientId` onto the real `Client.id` via `Device.clientId`, but only for genuine client-role devices, never a coach's own second device. Because it lives inside `applyPacket()` rather than in either caller, it applies identically whether the packet arrived over the network (`doCloudSync`) or as an imported `.cwsync` file (`DeviceRow`'s pre-existing "Local Import") — one merge fix, both transports fixed. That observation — *one merge point regardless of transport* — is what made the "how does this work across all three hosting tiers" question answerable cleanly rather than needing three special cases: built `exportLogsFile()` on the Companion side (identical payload to the network push, just handed back as text for a file download instead of a POST body) so a fully-local coach still gets a real, working sync path — the client exports a file, sends it any way they like, and it drops onto the coach's *existing* Local Import button with zero new coach-side code. `CoachCard.tsx` now shows "Sync now" only when a relay URL is on file and "Export packet" always, so the UI itself is honest about which transport actually applies. Wrote `docs/CLIENT_APP_STRATEGY.md` §7 formalizing this: one payload shape (`{tables: {sessionLogs, metrics}}`), one coach-side merge function (`applyPacket`), and a table of exactly what differs per tier (nothing except which of two transports carries the packet) — plus an explicit accounting of why this keeps a client's data manageable and transferable: it never leaves their own device except as this narrow synced subset, backup/restore is tier-independent, and switching (or losing) a coach is just replacing one `CoachLink` row with zero effect on local workout/metric history. **Verified live for the specific case not yet exercised** (debt #52's verification covered self-hosted/managed via network; this leg covered fully-local via file): paired with a synthetic coach leaving the relay URL blank, confirmed the tier-aware UI correctly showed only "Export packet," logged a workout, captured the real exported blob via a `URL.createObjectURL` intercept (not just "the button didn't throw"), decrypted it with the synthetic coach's key, and manually ran the exact `remapClientId` rewrite against the decrypted data to confirm it produces the right real `clientId`. 146/146 vitest green (coach app, +4 new), `tsc -b` clean on both apps, zero console errors. Debt #53 added (full writeup); #52 updated to note its one open gap is now closed.

- **S13 (Claude Fable 5, 2026-07-21):** Caleb's brief: finish the client-side story — how a client connects to a coach on any hosting setup, whether messaging exists without a server, P2P/WiFi when there's no server at all, tier migration, and "amazing cryptography" — plus UI appeal and doc currency. Shipped, all verified live in-browser against a real running relay: (1) **Coach→client delivery is now real (T14 done).** Coach packets (program + exercises + this client's Client row + messages) now reach Companion over ALL three transports through ONE new merge function, `applyCoachPacket()` (`companionSyncApi.ts`): relay pull (`GET /sync/pull/coach/{deviceId}`), the LAN response, or an imported `.cwsync` file — same `seq` replay-guard as the coach side (`CoachLink.lastSeqFromCoach`, confirmed rejecting a replayed packet live). New read-only **Program viewer** (`features/program/ProgramPage.tsx`, wire-format copies of the coach's `Week`/`Day`/`Block`/`SetPrescription` types + `fmtSet` in `lib/programFormat.ts` with 6 unit tests) renders assigned programs with real exercise names; verified a program pushed via relay, then updated via an imported file (Week 2 appeared, newest-wins). (2) **Fixed a real relay keying bug:** `doCloudSync` pushed EVERY device's coach packet under the coach's own device id — with 2+ clients each sync overwrote the packet awaiting the previous client. Now keyed per recipient (`id: device.id, type: 'coach'`), with `sync-server`'s `sync_payloads` moved to a composite `(id, type)` PK + in-place migration (verified live: client and coach payloads coexist under one id). (3) **Messaging now works on every tier, including fully-local.** `messages` added to both packet directions (`COACH_TO_CLIENT_TABLES` + Companion's outbound payload, each side shipping only rows IT authored — no echo loops), with ONE message id across local row / relay row / packet row (coach's `pushRelayMessage` now takes the local `CoachMessage.id`; all pull paths upsert-by-id instead of `create`) so mixing transports can't duplicate a thread — verified: same message delivered by relay AND packet landed as one row. Companion sends now save-first and queue when no relay exists ("Saved — goes out with your next sync"). (4) **WiFi/LAN sync from Companion (T1's app half) + a real bug fix:** new `syncOverLan()` POSTs the sealed packet straight to the coach desktop app's LAN endpoint and applies the coach's return packet from the same response (one tap = two-way sync, zero servers); found the S9 IPC response path was silently broken (`preload.ts` used `ipcRenderer.invoke` against `ipcMain.once` — the reply could never arrive, every LAN sync would have hung) — fixed with `send`/`ipcMain.on` + per-request `syncId` correlation + 30s timeout in `electron/main.ts`; verified live against a contract-exact stub (real Electron GUI two-device pass folded into T2, debt #54). (5) **Companion UI overhaul:** new Coach tab (`CoachPage.tsx`) with a real message thread, transport-aware sync controls (relay button only when configured, WiFi + file always), and honest status copy; Settings' `CoachCard` slimmed to pairing + editable addresses (relay/key/LAN — tier migration without re-pairing); Home now surfaces the assigned program, latest coach message, and last-sync date; 5-tab nav (Coach added). (6) **Docs:** `CLIENT_APP_STRATEGY.md` §7 gained the LAN transport row, plus a new **§8 security-model/FAQ** answering Caleb's exact questions (who pays for what per tier, "do clients just connect to the coach's server?", messaging-without-a-server, Personal Cloud × coach-relay composition, tier-switch friction, the full crypto description incl. honest limits — no forward secrecy ratchet, relay sees metadata). HANDOFF §3 gained 5 new S13 conventions (relay keying asymmetry, message-id invariant, authored-rows-only packet filter, invoke/handle IPC pairing, stub-verification pattern); queue re-pointed (T14 done, T1/T8 halves marked). **146/146 coach vitest + 6 new Companion tests green, `tsc -b` clean on both apps + electron, zero console errors through every browser pass.** Debts #54–55 added.
  **Continued, same session — QR-first pairing, managed-hosting runbook, battery/notifications doctrine + Web Push, mobile design overhaul.** Caleb asked: QR for first-time pairing, "determine the best way for us to create a managed server," WiFi/data sync without battery drain + background work/notifications/widgets, and a mobile design overhaul. Shipped, all verified live: (1) **QR-first pairing.** `PairingCode` gained optional backward-compatible transport hints (`relay`/`relayKey` — edited in BOTH `lib/sync.ts` copies identically); Studio Link now renders the pairing code as a QR that embeds the coach's relay address+key (with an explicit share-carefully note, shown only when the tier makes the relay usable), and Companion's `PairingFlow` is scan-first: a real camera scanner (`QrScanner.tsx`, platform `BarcodeDetector` + `getUserMedia`, zero decoding libraries, stream torn down on result) with graceful fallback to paste where unsupported (verified: desktop Chrome hides the scan button), the client's own code as a QR, and **transport autofill** — verified live end-to-end: pasted a hint-carrying code, safety number matched an independently computed one, "Server address came with the QR — sync is configured, nothing to type," and the saved `CoachLink` carried the relay+key. One scan = paired AND configured. (2) **Managed hosting is now a runbook, not a vibe:** new `docs/MANAGED_HOSTING.md` — reference deployment (one $5–8 VPS, Caddy TLS, SQLite+Litestream), env table, manual-first Stripe provisioning flow with the exact admin curl, capacity math (one box ≈ hundreds of coaches; the ceiling is ops, not compute), security posture incl. honest gaps (plaintext API keys in SQLite past ~50 coaches), and retention policy. Relay hardening implemented to match: `/health` (unauthenticated liveness), retention sweep (messages + sent reminders past `RELAY_RETENTION_DAYS`, default 90, boot+daily), and **Web Push**: VAPID keys env-or-generated-and-persisted (`kv` table), `/push/vapid`+`/push/subscribe`+`/push/unsubscribe`, and a metadata-only push ("New message — open Companion") fired on coach→client `/messages/push`, with dead-subscription pruning on 404/410 — all curl-verified against the live server. (3) **Battery doctrine, implemented then documented** (`CLIENT_APP_STRATEGY.md` §9): `lib/autoSync.ts` is event-driven ONLY (app open, `visibilitychange`, `online`; 15-min throttle; deliberately zero `setInterval`/sockets — a hidden app does no work), closed-app notifications are Web Push's job (`lib/push.ts` + `sw.js` push/notificationclick handlers + a Settings opt-in card), reminders now actually reach Companion (`pullReminders` closes debt #55's poll half; surfaced as "Reminder: …" messages), and §9.3 states plainly that widgets/guaranteed background need the Capacitor wrap — no fake widget feature. Push verified to the environment's limit (pane hard-denies notification permission): graceful-denial path confirmed, full grant→FCM delivery left as debt #57's one real-device check. (4) **Mobile overhaul (Companion):** found + fixed a real bug — the Settings theme picker was a **complete no-op** (nothing ever toggled `.dark`); `lib/theme.ts` now applies it live incl. a `system` media-query watcher (verified: pane renders dark end-to-end). Design pass: 44px minimum touch targets (`Button`/`Input` sized up; the unpair/delete/cancel icon buttons were genuinely un-tappable at 14px and got proper hit areas), safe-area insets top+bottom (notch + home indicator), frosted `backdrop-blur` bottom nav with an active pill + heavier active stroke, one `PageHeader` treatment across every page, `inputMode` numeric/decimal on all workout number fields, and a dependency-free `Sparkline` giving Progress a real trend chart (verified rendering with seeded data). Boot-hang note: the coach app's first load in a totally fresh profile hung at the first boot step and recovered on reload — logged as debt #54a, the strongest live sighting of long-standing debt #6. **146/146 + 6/6 vitest, `tsc -b` clean everywhere, zero console errors.** Debts #56–57 added; `qrcode.react` added to companion-app, `web-push` to sync-server.

- **S14 (Claude Opus 5, 2026-07-26):** Caleb's brief was two words — "finish this app" — so this session worked the HANDOFF queue to the end rather than adding anything new. **Phase 9 is now done**, and every remaining open task in §4 is either closed or explicitly and honestly declined. Shipped: (1) **The first-boot hang (debts #6/#54a) is fixed and the mechanism proven, not guessed** — `trainerRepo.getOrCreate()` and `seedExercisesIfEmpty()` were both check-then-insert, StrictMode ran the boot effect twice, both copies read "empty", both inserted, and the loser's ConstraintError rejected an un-caught promise chain, leaving the boot screen frozen forever. Added `singleFlight()` to `lib/core.ts` plus adopt-the-winner catches for the cross-tab case, and a real **boot error screen with Retry** so no future boot failure can present as a hang. Reproduced the old failure on demand in the browser against real IndexedDB to confirm the diagnosis; 11 new tests (`db/boot.test.ts`). **The identical bug was then found live in `companion-app`** — its genuine first launch rendered a permanently blank page — and fixed the same way. (2) **Route error boundaries** (`app/RouteError.tsx` on every route) — verified by forcing a crash in `ReportsPage` and watching it render inside the Shell with nav intact. (3) **Code splitting**: main chunk **820KB → 476KB** (150KB gz, first time under the 500KB warning); every route but Today/Clients is lazy, `ClientDetailPage` is its own 121KB chunk, and the exercise seed now lives behind `db/seed/index.ts` so its ~38KB is fetched only on the one boot that seeds. (4) **PWA** for the coach app (manifest, maskable icons, hand-rolled `sw.js`) — **verified by stopping the preview server outright and reloading**; the app came back fully with 34 assets cached. (5) **Reduced-motion pass** that fixed three real defects in the existing rule rather than rubber-stamping it (infinite animations looping at ~0ms; surviving delays leaving `both`-filled boot text invisible; no scroll-behavior override), and the decorative boot reveal is now skipped entirely. (6) **T13 — Companion Film Room**: `lib/pose.ts` copied verbatim, tracker + tracking hook ported with every reliability behaviour the coach app learned the hard way, mobile-first single-clip page, client-voiced summary (6 tests), and a "send to coach" that transmits **only the text** over the existing encrypted message channel while the video never leaves the phone. Model + wasm confirmed loading from Companion's own bundle. (7) **T11 — Electron**: native menu driving the router over IPC, window geometry persisted with an off-screen-monitor guard; **the first real Electron GUI launch this project has ever had**, which immediately caught a **broken splash screen** (`splash.html` loaded from `dist-electron/`, where `tsc` never copies it — every launch logged ERR_FILE_NOT_FOUND). (8) **Debt #56 closed** — coach-side reminder scheduling, verified by decrypting the sealed row out of the relay's own SQLite with the client's private key and confirming the client's `/reminders/due` poll returns it exactly once. Added `DELETE /reminders/:id` to `sync-server`. (9) **T6b** — per-rep results table and a PNG snapshot export (verified by decoding the produced blob and sampling pixels); **T6d was already done** in an untracked pass. (10) **T10 finished** — the surfaces flagged as genuinely unverified all checked at 375px and 768px; the dual-video stage does stack. (11) **T8/T1's `template.html` half declined on purpose (debt #59)** — its stated premise was false (the export injects no keys), and the only ways to deliver it are baking a private key into an emailed file or duplicating the pairing handshake in vanilla JS. Instead removed a WiFi Sync button that **could never have worked** (plaintext + wrong id, failing at two layers) along with the `html5-qrcode` **CDN script** it needed, restoring the offline guarantee to a file whose whole promise is offline. (12) Release prep: `HOW-TO-OWN-IT.md`, debts #19 and #27 cleared. **157/157 coach vitest (+11), 12/12 companion (+6), `tsc -b` clean on coach + companion + electron, production builds clean with no chunk-size warning.** New debts #57b–62 recorded, including the two Phase 9 items deliberately not faked (#58: no Lighthouse or cross-browser run in this environment) and the honest state of Companion Film Room on real hardware (#61).

- **S15 (Claude Opus 5, 2026-07-27/28) — v2 planning package + first Phase-A foundations.** Caleb asked for a full v2 plan (connectivity, local AI, science engines, Film Room occlusion, exercise library, editions/pricing, i18n/Ctrl+K/polish, and Claude Design prompts) with **nothing built until the plan existed**, then green-lit implementation. **(1) Planning:** ten documents in `docs/plans/` (~17k words) covering all of it, including four ready-to-paste Claude Design prompts. Four decisions taken: **build WebRTC P2P *and* LAN discovery** (with the honest signalling/TURN caveats designed in, not glossed); **no fixed local-AI download cap** — hardware probe recommends the comfortable tier, edition gates which models are licensed; **~3,000 fully-curated exercises instead of 25,000 generated variants** (modifiers move to set-level prescription, which also stops fragmenting a client's history across near-duplicate rows); **$249 one-time / $179 launch with lifetime free updates** — no paid version upgrades, funded instead by new sales + optional cloud + community-contribution credits, with the revenue exposure stated plainly. Two places the plan deliberately claims *less* than asked: cycle-phase-based programming isn't supported by the meta-analyses (McNulty 2020, Colenso-Semple 2023), so the plan builds symptom-driven autoregulation plus RED-S/iron screening instead; and ACWR ships with its methodological critiques attached. **(2) Library authoring deferred by Caleb** — `docs/plans/05a-LIBRARY-AUTHORING-BRIEF.md` is a self-contained pinned handoff for a later session. **(3) Built:** `lib/edition.ts` (Personal/Independent/Studio capability flags; a missing or corrupt licence falls back to the *most restrictive* edition, never unlocks by accident) + `Trainer.edition/licenseKey/licensedSeats`. **Quick Log** (`lib/quickLog.ts` + `features/logging/QuickLogDialog.tsx`, Ctrl+K and Ctrl+L): type a set the way you'd say it — *"sam 3x5 225 back squat rpe 8"* — with two hard rules, **it asks rather than guesses** (an ambiguous "sam" across Sam Rivera/Samantha Cole produces a question and keeps "Log it" disabled) and **the client card renders above the input before anything is written** (avatar, last session, active program, injury ribbon). Fully deterministic — no AI needed; a local model would only pre-normalise messier phrasing into the same shape and can never skip the confirmation. Verified live end-to-end: disambiguation, card-above-input (confirmed geometrically), correct client written, same-day second exercise **appended to one log rather than duplicated**, unambiguous names skipping the question, quoted notes extracted. **(4) Import audit:** `madge` — no circular dependencies across 121 files; new `src/app/routes.smoke.test.ts` imports every routed module and asserts the export shape the router expects, plus a drift check parsing `router.tsx` via `?raw`. **It immediately caught a real bug**: `WiFiSyncDialog.tsx` read `window` at module scope, so importing `ClientDetailPage` threw in any non-DOM context — fixed with a `typeof window` guard. Also fixed a `source: 'manual'` → `'trainer'` bug the typechecker caught in Quick Log (`DataSource` is `'trainer' | 'companion-import'`). Added `npm run lint:imports` / `npm run verify`; madge runs via `npx` because it caps its TypeScript peer at `^5.4.4` while this project is on `~6.0.2` — the same ERESOLVE class as debt #49, deliberately not force-resolved. **224/224 vitest green (was 157), `tsc -b` clean, no circular deps.** Debt #63 added.

- **S15 continued — Phase A foundation: the transport seam is in.** Connectivity plan step 1, built and wired rather than left as an unused abstraction. `lib/sync/transport.ts` defines the `Transport` contract (probe/exchange, capabilities, cost-ordered preference LAN→P2P→relay→file) and `lib/sync/broker.ts` implements `syncWith()` — best-first selection, fall-through when a transport is unreachable *or* fails mid-exchange, a non-throwing `probeAll()` for the connection indicator, and exactly one honest sentence for the user in every outcome. Both are free of `features/`/`db/` imports so the broker is unit-testable with injected fakes: **16 tests**, covering preference order, mid-exchange fallthrough, a throwing probe not killing the loop, `automaticOnly` refusing to "succeed" via a path that needs a human, and the message wording for each case. `features/sync/transports/relayTransport.ts` extracts the inline fetch logic out of `SyncCenterPage`'s `doCloudSync` **preserving the asymmetric keying exactly** (push under the recipient's device id; pull from `client/{device.id}` vs `coach/{ourId}` by role — the S13 bug). `SyncCenterPage` now calls `syncWith()` and no longer knows a relay exists; `DeviceRow` lost its `identity` prop since the transport resolves it itself. **Verified live against a real running relay, both paths:** success pushed a 132KB sealed `CWSYNC1` packet under the recipient device id (confirmed in the relay's own SQLite) and reported *"Nothing new — synced over relay."*; pointing at a dead relay degraded to *"Couldn't reach this device. It'll sync automatically next time you're both online — nothing is lost."* **240/240 vitest green (was 224), `tsc -b` clean, no circular deps.** LAN and P2P now slot in behind the same interface with no edit to any feature component — which was the entire point.

- **S15 continued — i18n foundation (Phase A's last blocker).** Built the layer, converted a proving slice, and left the rest as mechanical follow-up rather than half-converting 70 components. **Library decision, recorded because it will otherwise look like laziness:** i18next's TS peer range (`^5 || ^6 || ^7`) *is* compatible — this was not forced by a conflict. It's hand-rolled because the genuinely hard parts of i18n are `Intl.PluralRules` / `Intl.DateTimeFormat` / `Intl.NumberFormat`, which are built in and correct for every locale by definition; what's left is lookup, interpolation and lazy loading (~200 lines), and the alternative was ~40KB of runtime against a protected 476KB budget, in a codebase that already hand-rolls its service worker, fuzzy search and charts for the same reason. Revisit if message *contexts*, gender selection, or a PO-file translator pipeline are ever needed. **Design:** `lib/i18n/core.ts` (pure, 23 tests) + `lib/i18n/index.tsx` (provider/hook). Plurals are a CLDR-category object, not a hand-built `n === 1` — tests assert Polish's one/few/many and Arabic's zero/two/few actually work, which is the bug hand-rolled i18n normally ships with. `#` expands **only inside plural messages** (ICU's rule) so ordinary copy like "Ranked #1" survives — caught while fixing a test. `locales/en.ts` is TypeScript, not JSON, so the key union is derived from it and `t('nav.clinets')` is a compile error rather than a string that renders as itself across 70 files. Catalogues are discovered with `import.meta.glob`, so shipping a language is exactly "add `locales/xx.json`" — a hardcoded map would either reference files that don't exist (build error) or claim languages we haven't translated (a lie); `availableLocales()` tells the picker the truth. A missing key falls back to English, never a raw key; a failed catalogue load falls back to English rather than blanking; `useTranslation()` outside a provider returns an English implementation rather than throwing, because print routes and the TV display render outside the shell. **Converted:** `AppRoot` (provider), `Shell` (nav + the backup indicator, which is the interesting case — its "Backup 3 days ago" is now properly pluralised). **Verified live:** switching to `es` lazily loaded the catalogue, set `lang="es"`, translated the entire nav with zero English leaking, and correctly fell back to English for keys Spanish doesn't have yet. `es.json` is committed as a **clearly-marked seed translation needing native review**, not as a finished locale. **263/263 vitest green (was 240), `tsc -b` clean.** Debt #64 added for the remaining ~65 components + the RTL layout pass.

- **S15 continued — RTL closed, and Phase B opened by fixing a real honesty defect.** **(1) RTL is done.** Added `ar.json` (seed, marked for native review) purely so RTL could actually be *seen* rather than assumed, then converted ~130 physical CSS classes to logical properties across 37 files (`ml→ms`, `mr→me`, `pl→ps`, `pr→pe`, `left→start`, `right→end`, `border-l/r→s/e`, `text-left/right→start/end`). **The regex corrupted prose and it was caught:** `left-facing` became `start-facing` in `Guide.tsx` and `left-aligned` in `Logomark.tsx` — found by grepping for `start-|end-` followed by letters rather than valid Tailwind values, and restored. Verified live in Arabic: `dir="rtl"`, sidebar flips to x=1110 with main at x=0, the active-nav verde spine resolves via `inset-inline-start`, zero horizontal overflow across 8 routes. **(2) Phase B — ACWR reworked for honesty.** Grepped before building (per this project's own standing lesson) and found `lib/trainingLoad.ts` already existed with ACWR/monotony/strain — **and containing a genuine defect**: it reported a "sweet spot" and a `danger` zone claiming *"the range associated with markedly higher injury risk"*. That does not survive its own literature — **Impellizzeri et al. 2020** and **Lolli et al. 2019** show the ratio is mathematically coupled to its own denominator and that the sweet-spot bands are substantially a construction artefact. A coach reading "danger" and deloading a healthy athlete is a real cost of overclaiming. Reworked: **EWMA estimator (Williams et al. 2017)** replacing rolling averages, **descriptive zones** (`insufficient-data | below-norm | steady | rising | sharp-rise` — no `danger`, no `sweet-spot`), a `reliable` flag that refuses to report a ratio under 21 days / 5 logged sessions rather than dressing noise as measurement, rewritten copy that describes load change and never predicts injury, and amber-not-red in `CoachingTab` so the UI doesn't make the claim the engine declines to. **A regression test now asserts no zone copy ever matches /injury|danger|risk of|unsafe/** — the doctrine is enforced, not just documented. **265/265 vitest green (was 263), `tsc -b` clean.**

- **S15 continued — Readiness v2 (Phase B).** The v1 score graded every athlete against one fixed scale, which is why most readiness scores feel wrong: someone who has slept 6 hours a night for fifteen years is *normal for them*, but a population threshold flags them amber forever until the coach learns the number means nothing. **v2 z-scores each signal against that client's own 28-day history** (Bourdon et al. 2017 IJSPP consensus; Hooper & Mackinnon 1995; McLean 2010; Saw et al. 2016), and **refuses to score at all until 14 check-ins exist** — showing a confident number from three would be inventing precision, and the first time a coach catches it being wrong every number in the app loses credibility. Output is score + per-domain SD + the driving domain + a recommendation, so the coach sees *why*, not just a number. Wired into `CheckInsTab`'s card, which now renders an honest "still learning — N more check-ins needed" state. **Two real defects were found and fixed during the build, both by testing rather than by reading:** (1) a perfectly consistent client has ~0 measured SD, so the first implementation reported *any* deviation as z=0 — a 9h sleeper dropping to 7h was invisible; fixed with a per-domain **minimum-spread floor** (the Hopkins/Buchheit smallest-worthwhile-change idea) so real drops stay detectable without dividing by zero. (2) That floor then produced **"10 SD below normal"** in the live UI — mathematically fine, but it reads as broken and quotes a precision we don't have; capped at ±3 SD, and beyond the cap the copy switches to "far below their normal" rather than quoting a figure we don't believe. **The second one was only visible in the browser** — the unit fixtures had unrealistically flat histories, which is exactly the case the cap exists for. Also noted: a stale in-memory module survived both a force-navigate and a dev-server restart, and only a genuine `location.reload()` picked up the fix — worth remembering before concluding a change "didn't work". **281/281 vitest green (was 265), `tsc -b` clean.**

- **S15 continued — Nutrition v2: Energy Availability & REDs screening (Phase B).** The feature that turns the nutrition tab from a calculator into a **safety instrument**, and the most defensible thing in the whole v2 plan. `lib/energyAvailability.ts` computes `EA = (intake − exercise energy expenditure) / fat-free mass` and screens it against the consensus thresholds (≥45 optimal, <30 = low energy availability), plus a REDs indicator screen. Sources: **Loucks 2011**, **De Souza et al. 2014** (Female Athlete Triad Coalition), **Mountjoy et al. 2023** (IOC REDs consensus). **20 tests, green first run.** Four honesty rules are built in and enforced by tests, because this is health-adjacent and overclaiming here is genuinely harmful: **(1) it screens, it never diagnoses** — low EA sets a `referral` flag rather than producing a fix; **(2) it reports a RANGE, never a single confident number**, because self-reported intake is systematically unreliable by 10–30% and a point estimate would overstate what we know; **(3) it refuses to compute without a real body-fat measurement** rather than estimating FFM, since a wrong FFM silently corrupts every downstream number — the UI says so plainly instead of showing a false all-clear; **(4) confidence is lowered for men**, because Mountjoy 2023 is explicit that the 30 kcal/kg threshold's evidence base is strongest in exercising women. `screenReds()` refers immediately on any disordered-eating flag regardless of the arithmetic — that is outside coaching scope and the cost of under-reacting dwarfs the cost of an unnecessary referral. **Wired into `NutritionTab`** (body fat sourced from the existing `bodyfat` metric, not invented) with the prescription warning rendered *above* the macros, because a target that drives low EA outranks every other number on the page. **Verified live both ways:** a lean female athlete on a cut produced EA 27 → stop-level warning naming the threshold and recommending referral; deleting her body-fat metric switched the card to "add a body-fat percentage… an estimate would be worse than nothing" and correctly withdrew the warning rather than guessing. **301/301 vitest green (was 281), `tsc -b` clean.**

- **S15 continued — Cycle & symptom tracking: the science layer (Phase B).** `lib/cycle.ts` + **26 tests**, and the module deliberately does **less** than the obvious version of this feature — which is the entire point. **Phase-based training prescription is not supported by the evidence** (McNulty et al. 2020 meta-analysis: trivial average differences, low-quality evidence, extreme heterogeneity; Colenso-Semple et al. 2023: current evidence does not support phase-based prescription; Elliott-Sale et al. 2021 on the methodological standards most of the literature fails). So instead: **symptom burden feeds readiness as a symptom**, exactly like any other subjective input, capped at −2 so one input can't swamp a score that also weighs sleep, energy and load. What IS well supported is implemented — **heavy bleeding → iron-status prompt** (Bruinvels 2016/2021; Sim 2019), **cycle disruption → routes to the energy-availability/REDs screen** built earlier this session (Mountjoy 2023) rather than to programming, and **contraceptive context as reassurance not a lever** (Elliott-Sale 2020: negligible-to-small effects). `personalPattern()` surfaces the client's *own* recurring pattern explicitly labelled **n=1**, requiring 3+ cycles and 60% recurrence before it says anything — honest where a population claim would not be. **Two doctrine tests enforce this**: no guidance string may match `/follicular|luteal|ovulat|phase/i`, and `CYCLE_FRAMING` (the shared copy) must keep stating the evidence honestly and must not match `/train heavier|optimal phase|best time to/i` — so a future session can't quietly swap in a phase-based claim without editing the file and reading its header. **One real design flaw caught by a failing test:** banding was purely additive, so debilitating cramps-and-nothing-else (total 3) banded as *mild* and would have told a coach to push a session that should have been cut; now the worst single symptom is weighed alongside the total, and one symptom at maximum severity is never mild. **Privacy (plan §4.5):** this is special-category health data. The sync layer already uses **explicit allowlists** (`COACH_TO_CLIENT_TABLES` / `CLIENT_TO_COACH_TABLES`), so a cycle table is excluded by default — that architecture was already right, and the module header says in as many words not to add it. **327/327 vitest green (was 301), `tsc -b` clean.** **NOT built yet** (task #9): the `cycleEntries` schema migration, the Companion-side logging UI, the consent moment, and the coach-side aggregate-only view.

- **S15 continued — Nutrition v2: the athlete-grade layer (Phase B).** `lib/nutritionAdvanced.ts` + **22 tests, green first run**, kept as a separate composable module so the well-tested `lib/nutrition.ts` didn't need destabilising. Three pieces, all wired into `NutritionTab` and verified live: **(1) Better energy equations.** Mifflin-St Jeor stays the correct default (Frankenfield 2005) but knows nothing about body composition, so it systematically under-predicts for lean clients. `chooseBmr()` switches to **Katch-McArdle** or **Cunningham** *once body fat is actually measured*, and refuses to guess FFM when it isn't — verified live on a lean 45-year-old male at 11% body fat: **Cunningham 2106 kcal vs Mifflin 1725, a 381 kcal/day correction**, with the reasoning shown so the coach can defend it. **(2) Protein as a DISTRIBUTION, not a daily total** (Schoenfeld & Aragon 2018; Moore et al. 2015; Helms 2014; Rogerson 2017) — per-meal dosing, a raised per-meal floor for 40+ (anabolic resistance: it's the per-*meal* dose that changes with age, not just the total), a plant-based uplift with the leucine reasoning, and an explicit warning when the chosen meal count leaves each feeding too small to trigger the response. **(3) Carbohydrate periodised to the day's actual training** ("fuel for the work required" — Burke et al. 2011; ACSM/AND/DC 2016; Jeukendrup 2014), closing the biggest gap in v1 for endurance athletes, where one flat daily number is simultaneously too much on a rest day and far too little before a long session. Verified live: 410–574 g on a moderate day → 656–984 g on a very-high day, with the **90 g/h multiple-transportable-carbohydrate ceiling** (and its gut-adaptation caveat) appearing only on sessions long enough for it to matter. `loadToSessionBand()` maps sRPE load onto the band so this can run off logged training instead of asking the coach to categorise every day. **349/349 vitest green (was 327), `tsc -b` clean.**

## Update protocol (every AI session MUST do this)
1. On start: read `HANDOFF_SONNET.md`, then this file, then spec sections for your phase.
2. On finish: update the table above, append to Session log, list new debts honestly, re-run build + tests and paste results into the session log line.
